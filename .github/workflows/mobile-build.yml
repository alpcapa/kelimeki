# Mobil (Flutter) derleme doğrulaması.
#
# NEDEN VAR: geliştirme ortamında (Claude Code oturumu) Android SDK, Xcode ve
# CocoaPods YOK — `flutter test` koşuyor ama `flutter build ios/apk` hiç
# koşmadı. Yani "testler yeşil" ile "uygulama gerçekten derleniyor" arasında
# doğrulanmamış bir boşluk vardı; özellikle platform eklentileri
# (sqflite, shared_preferences, supabase_flutter, share_plus, path_provider)
# ancak gerçek bir pod/gradle derlemesinde patlar.
#
# MALİYET: bu depo PUBLIC olduğundan Actions dakikaları ücretsiz ve
# sınırsız (macOS runner'ları dahil) — özel depolardaki 10x macOS çarpanı
# burada geçerli DEĞİL. Bu yüzden mobil dosyalara her dokunuşta çalışıyor;
# derleme kırılırsa aynı gün görülür.
#
# TETİKLEYİCİLER (12 Ağustos 2026'da değişti — aşağıdaki notu OKU):
#   (1) `main`e `mobile/**` push'u — merge sonrası tam boru hattı; Appetize
#       ve GitHub Pages test ortamı YALNIZCA burada güncellenir.
#   (2) `main`e açılan, `mobile/**` dokunan PR — merge ÖNCESİ doğrulama;
#       derler ama HİÇBİR ŞEY YAYINLAMAZ (bkz. "YAYINLAMA" notu).
#   ⚠ İkisinde de `mobile/**.md` HARİÇ (2 Eylül 2026) — gerekçe `on:`
#     bloğundaki notta.
#   (3) elle (Actions → Run workflow).
#
# NEDEN DEĞİŞTİ: tetikleyici, artık SİLİNMİŞ olan port dalına
# (`claude/word-game-flutter-port-dr9dos`) bağlıydı. Port 12 Ağustos'ta
# main'e merge edilince o dal yok oldu ve workflow SESSİZCE ölü tetikleyiciye
# bakmaya devam etti — yani mobil değişiklikler hiçbir gerçek derlemeden
# geçmeden main'e girmeye başladı (PR #241 tam olarak böyle girdi: yalnızca
# `flutter analyze` + testler koşmuştu, `build apk`/`build ios` HİÇ
# koşmamıştı). Bu, workflow'un var olma sebebinin ta kendisiydi.
# **Ders: bir workflow'u belirli bir dala bağlarken, o dal öldüğünde
# workflow'un sessizce hiçbir şey yapmayacağını hesaba kat** — kırmızı bir
# CI değil, HİÇ ÇALIŞMAYAN bir CI üretir ve bu fark edilmesi çok daha zordur.
#
# YAYINLAMA — PR'larda BİLİNÇLİ OLARAK KAPALI: `mobile-latest` release'i,
# Appetize app'leri ve GitHub Pages sitesi TEK ve PAYLAŞILAN kaynaklar
# ("en son test derlemesi" anlamına geliyorlar). Bir PR bunları güncellerse
# henüz merge EDİLMEMİŞ kod "en son" diye görünür ve cihazda test eden kişi
# neyi test ettiğini bilemez. Bu yüzden PR'da yalnızca DERLEME koşuyor;
# release/Appetize adımları ve Pages işi atlanıyor.
#
# İMZALAMA/TESTFLIGHT: 8 Eylül 2026'da EKLENDİ (FAZ C 24.2) — aşağıdaki
# "TestFlight" adımı. Aşağıdaki iki paragraf üyelik ALINMADAN ÖNCEKİ durumu
# anlatıyor ve hâlâ doğru: imzasız iki derleme DURUYOR, Appetize akışı
# bozulmadı, ve iOS'u tarayıcıda test etmek için hâlâ üyelik gerekmiyor.
# iOS işi iki çıktı üretiyor, ikisi de imzasız: (a) `--no-codesign` cihaz
# derlemesi — yalnızca "derleniyor mu" kanıtı; (b) SİMÜLATÖR `.app`'i —
# Appetize.io'ya yüklenip tarayıcıda çalıştırılabiliyor. Apple Developer
# üyeliği yalnızca GERÇEK CİHAZA kurulum (TestFlight/App Store) için
# gerekli; o adım 8 Eylül 2026'da EKLENDİ (yukarı bkz.) — bu cümle bir
# süre "üyelik geldiğinde eklenecek" diyordu ve 9 Eylül'de düzeltildi
# (bkz. mobile/TESTING.md,
# "TestFlight kurulumu").
#
# APPETIZE'A OTOMATİK YÜKLEME (7 Ağustos 2026): her Android/iOS derlemesi
# bittiğinde, `APPETIZE_API_TOKEN` secret'ı varsa, GitHub Release'e az önce
# yüklenen dosyanın URL'i Appetize'ın REST API'sine POST edilip aynı iki
# Appetize app'i (sabit public-key) YERİNDE günceller — geliştirici iPad'den
# çalıştığından ve dosya seçici/sürükle-bırak ile .apk'yı elle yükleme
# günlerce "400"/"silik dosya" hatasına takıldığından (bkz. mobile/TESTING.md,
# "Appetize — otomatik yükleme"), bu adım o dansı tamamen ortadan kaldırıyor.
# Sabit oturum linkleri: mobile/TESTING.md.
name: Mobil derleme

on:
  push:
    branches: [main]
    paths:
      - 'mobile/**'
      # ⚠ YALNIZCA DOKÜMAN DEĞİŞEN BİR PR macOS/Android DERLEMESİ BAŞLATMASIN
      # (2 Eylül 2026). `mobile/**` deseni `mobile/CLAUDE.md`,
      # `mobile/TESTING.md`, `mobile/docs/*.md`yi de yakalıyordu; o gün tek
      # satır doküman değiştiren bir PR (#405) iOS derlemesini tetikledi.
      # Deponun kendi ilkesiyle çelişiyordu — bkz. `docs-size.yml`in başındaki
      # "bu repoda 'yalnızca doküman değişikliği ücretsizdir' kuralı bilerek
      # korunuyor" notu.
      #
      # NEGATİF DESENİN SEMANTİĞİ (yanlış anlaşılırsa CI SESSİZCE ÖLÜR, bu
      # deponun kayıtlı en kötü arıza tipi — bkz. başlıktaki ölü dal vakası):
      # GitHub her DEĞİŞEN DOSYAYI listeyle eşleştirir ve SON eşleşen desen
      # kazanır; dosyalardan EN AZ BİRİ dahil kalıyorsa workflow koşar. Yani
      # `.dart` + `.md` birlikte değişen bir PR'da `.dart` hâlâ dahil ve
      # derleme KOŞAR. Dışarıda kalan tek şey `.md`nin TEK BAŞINA olduğu hâl.
      # `**.md` GitHub'ın kendi dokümanındaki idiom (`**.js` örneği) ve alt
      # dizin gerektirmez, yani `mobile/CLAUDE.md` de kapsanır.
      - '!mobile/**.md'
      - '.github/workflows/mobile-build.yml'
  pull_request:
    branches: [main]
    paths:
      - 'mobile/**'
      - '!mobile/**.md'  # push ile AYNI — yukarıdaki gerekçeye bkz.
      - '.github/workflows/mobile-build.yml'
  workflow_dispatch:
    inputs:
      ios:
        description: 'iOS derlemesi (macOS runner)'
        type: boolean
        default: true
      android:
        description: 'Android APK derlemesi'
        type: boolean
        default: true
      web:
        description: 'Web derlemesi (GitHub Pages test ortamı)'
        type: boolean
        default: true

# Aynı anda tek çalıştırma yeter; arka arkaya tetiklenirse eskisini iptal et.
concurrency:
  group: mobile-build-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    name: Analiz + testler
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true
      # Saf Dart motor paketi: golden vector replay testleri (web motoruyla
      # parite kanıtı). Flutter'a hiç bağlı değil, `dart` ile koşuyor.
      - name: kelimeki_core testleri
        working-directory: mobile/kelimeki_core
        run: |
          dart pub get
          dart run test/run_all.dart
      - name: Uygulama analiz + testleri
        working-directory: mobile/app
        run: |
          flutter pub get
          dart analyze lib/ test/
          flutter test
      # PR'da web İŞİ atlanıyor (o iş yalnızca Pages'e deploy etmek için var,
      # bkz. yukarıdaki "YAYINLAMA" notu) — ama web'e ÖZGÜ derleme
      # kırılmaları gerçek bir sınıf: `Isolate.run` web'de yok, sqflite'ın
      # platform kanalı yok, `sqflite_common_ffi_web` koşullu import'un
      # arkasında (bkz. mobile/CLAUDE.md, "Web Derlemesi"). Bunlar
      # `flutter test`te GÖRÜNMEZ. Deploy etmeden derleyip kontrol ediyoruz.
      - name: Web derlemesi kontrolü (PR — deploy YOK)
        if: ${{ github.event_name == 'pull_request' }}
        working-directory: mobile/app
        run: flutter build web --release --no-web-resources-cdn --pwa-strategy=none --base-href /kelimeki/

  android:
    name: Android APK
    if: ${{ github.event_name != 'workflow_dispatch' || inputs.android }}
    runs-on: ubuntu-latest
    # Release'e yükleme için (aşağıdaki adım) yazma izni şart.
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v7
      # Derleme kimliği — arayüzdeki teşhis satırına gömülür ki bir ekran
      # görüntüsü "hangi commit çalışıyor" sorusunu KENDİ BAŞINA yanıtlasın
      # (bkz. mobile/CLAUDE.md, "Deploy Doğrulaması"). `pull_request`
      # olayında `github.sha` MERGE commit'idir — gerçek head'i kullan,
      # yoksa ekrandaki sha hiçbir dalda bulunmayan bir commit'i gösterir.
      - id: stamp
        run: |
          SHA=$(git rev-parse --short "${{ github.event.pull_request.head.sha || github.sha }}")
          echo "sha=$SHA" >> "$GITHUB_OUTPUT"
          echo "time=$(date -u '+%d.%m %H:%M')" >> "$GITHUB_OUTPUT"
      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '17'
      - uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true
      # Anahtarlar verilmezse uygulama TAMAMEN OFFLINE moda düşer (web'deki
      # `configured` bayrağının eşleniği) — derleme doğrulaması için yeterli.
      # Gerçek bir test derlemesi isteniyorsa depo sırlarına eklenir.
      - name: APK derle
        working-directory: mobile/app
        run: |
          flutter pub get
          flutter build apk --release \
            "--dart-define=BUILD_SHA=${{ steps.stamp.outputs.sha }}" \
            "--dart-define=BUILD_TIME=${{ steps.stamp.outputs.time }}" \
            --dart-define=SUPABASE_URL=${{ secrets.SUPABASE_URL }} \
            --dart-define=SUPABASE_ANON_KEY=${{ secrets.SUPABASE_ANON_KEY }}
      # İnternet izni BİRLEŞMİŞ manifeste girdi mi? (24 Ağustos 2026)
      #
      # Kaynak manifesti grep'lemek YETMEZ: izin oraya yazılmadan da bir
      # eklentinin manifestinden gelebilir, ya da tam tersi bir merger kuralı
      # onu düşürebilir. Soru "yazdık mı" değil "PAKETE girdi mi" — bu yüzden
      # kontrol derleme çıktısını okuyor (aşağıdaki .aab imzasını GERİ OKUyan
      # adımla aynı felsefe).
      #
      # NEDEN VAR: izin `main/` manifestinde yoktu, yalnızca `debug/` ve
      # `profile/`de vardı — yani release APK internetsiz çıkıyor ve sunucuya
      # dokunan HER özellik (giriş, Canlı oyun, k-lig, sohbet) sessizce
      # ölüyordu. Hata mesajı DNS hatasına benzediğinden (`Failed host lookup`)
      # teşhisi de yanlış yöne çekiyordu. İlk gerçek cihaz kurulumunda çıktı.
      #
      # POST_NOTIFICATIONS aynı sınıftan ve 28 Ağustos 2026'da (push bildirimleri,
      # Parça 158) bu listeye eklendi: izin birleşmiş manifeste girmezse Android
      # 13+ bildirimi HİÇ göstermez ve hiçbir hata düşmez — sunucu "gönderdim"
      # der, cihazda hiçbir şey olmaz. Tam olarak INTERNET'in sessiz kaybı.
      - name: İzinler pakete girdi mi (INTERNET · POST_NOTIFICATIONS)
        working-directory: mobile/app
        run: |
          set -euo pipefail
          MERGED=$(find build/app/intermediates -path '*merged_manifest*' \
                     -path '*release*' -name AndroidManifest.xml)
          if [ -z "$MERGED" ]; then
            echo "::error::Birleşmiş release manifesti bulunamadı — kontrol sessizce geçemez."
            exit 1
          fi
          FAIL=0
          for m in $MERGED; do
            for izin in INTERNET POST_NOTIFICATIONS; do
              if grep -q "android.permission.$izin" "$m"; then
                echo "ok ($izin): $m"
              else
                echo "::error::$izin izni YOK: $m"
                FAIL=1
              fi
            done
            # ⚠ OLMAMASI gereken izin. `firebase_analytics` kendi manifestinde
            # AD_ID bildiriyor ve birleşme onu sessizce içeri alıyor; Play'deki
            # "Advertising ID kullanılmıyor" beyanımız o an YANLIŞA düşüyor ve
            # bu, canlı bir uygulamada politika uyarısı sebebi. Manifestte
            # `tools:node="remove"` ile kaldırılıyor — bu kontrol, o satır
            # silinirse ya da yeni bir kütüphane izni geri getirirse derlemeyi
            # düşürür. Beyanla kodun ayrışmasını insan gözüne bırakma.
            if grep -q "com.google.android.gms.permission.AD_ID" "$m"; then
              echo "::error::AD_ID izni birleşmiş manifeste girmiş: $m — Play'deki 'Advertising ID kullanılmıyor' beyanı yanlışa düşer"
              FAIL=1
            else
              echo "ok (AD_ID yok): $m"
            fi
          done
          exit $FAIL
      - uses: actions/upload-artifact@v7
        with:
          name: kelimeki-apk
          path: mobile/app/build/app/outputs/flutter-apk/app-release.apk
          if-no-files-found: error
      # ---- Play Store paketi (.aab) — ROADMAP FAZ B → 0.A2 -------------------
      # Play `.apk` KABUL ETMİYOR; yüklenebilir tek biçim App Bundle. Yukarıdaki
      # `.apk` adımı DURUYOR — Appetize test ortamı yalnızca ham `.apk` alıyor.
      #
      # Adım YALNIZCA `ANDROID_KEYSTORE_BASE64` secret'ı varken iş yapar.
      # `secrets` bağlamı adım düzeyinde `if:`te KULLANILAMAZ (GitHub'ın bağlam
      # tablosunda yok) — bu yüzden kontrol, Appetize adımındaki gibi script'in
      # İÇİNDE; sonuç bir output ile artefakt adımına taşınıyor.
      #
      # `--build-number` = koşu numarası: Play aynı versionCode'u İKİ KEZ kabul
      # etmiyor, `pubspec.yaml`'daki `+N` ise her derlemede aynı kalırdı.
      #
      # `--dart-define`ler TIRNAK İÇİNDE — BUILD_TIME boşluk taşıyor ve
      # tırnaksız hâli 15 Ağustos 2026'da Android işini düşürmüştü.
      - name: Play paketi (.aab) derle
        id: aab
        working-directory: mobile/app
        env:
          ANDROID_KEYSTORE_BASE64: ${{ secrets.ANDROID_KEYSTORE_BASE64 }}
          ANDROID_KEYSTORE_PASSWORD: ${{ secrets.ANDROID_KEYSTORE_PASSWORD }}
        run: |
          set -euo pipefail
          if [ -z "${ANDROID_KEYSTORE_BASE64:-}" ]; then
            echo "ANDROID_KEYSTORE_BASE64 secret'ı yok — .aab derlenmiyor."
            echo "built=false" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          KS_FILE="$RUNNER_TEMP/kelimeki-upload.jks"
          printf '%s' "$ANDROID_KEYSTORE_BASE64" | tr -d '[:space:]' | base64 -d > "$KS_FILE"
          # `key.properties` rootProject (= mobile/app/android) kökünde aranıyor;
          # `storeFile` MUTLAK yol olmalı — göreli verilirse `app/` modülüne göre
          # çözülür (bkz. android/key.properties.example).
          printf 'storeFile=%s\nstorePassword=%s\nkeyAlias=upload\nkeyPassword=%s\n' \
            "$KS_FILE" "$ANDROID_KEYSTORE_PASSWORD" "$ANDROID_KEYSTORE_PASSWORD" \
            > android/key.properties
          flutter pub get
          flutter build appbundle --release \
            "--build-number=${{ github.run_number }}" \
            "--dart-define=BUILD_SHA=${{ steps.stamp.outputs.sha }}" \
            "--dart-define=BUILD_TIME=${{ steps.stamp.outputs.time }}" \
            --dart-define=SUPABASE_URL=${{ secrets.SUPABASE_URL }} \
            --dart-define=SUPABASE_ANON_KEY=${{ secrets.SUPABASE_ANON_KEY }}
          # Paketin imzasını GERİ OKU. `key.properties` yoksa Gradle release'i
          # DEBUG anahtarına düşürüyor (bilinçli — bkz. app/build.gradle.kts) ve
          # öyle bir .aab sessizce üretilip ancak Play'de reddedilirdi. Bu adım o
          # sessizliği kapatıyor: paketin parmak izi, az önce çözülen upload
          # anahtarınınkiyle AYNI olmak zorunda — SABİT bir değer YAZILMIYOR,
          # anahtar değişirse kontrol kendiliğinden onu takip eder.
          AAB=build/app/outputs/bundle/release/app-release.aab
          EXPECTED=$(keytool -list -v -keystore "$KS_FILE" -alias upload \
            -storepass "$ANDROID_KEYSTORE_PASSWORD" 2>/dev/null \
            | grep -m1 'SHA256:' | tr -d '[:space:]' || true)
          ACTUAL=$(keytool -printcert -jarfile "$AAB" 2>/dev/null \
            | grep -m1 'SHA256:' | tr -d '[:space:]' || true)
          echo "beklenen: $EXPECTED"
          echo "paket   : $ACTUAL"
          if [ -z "$EXPECTED" ] || [ -z "$ACTUAL" ] || [ "$EXPECTED" != "$ACTUAL" ]; then
            echo "HATA: .aab upload anahtarıyla imzalanmamış (debug anahtarı?)."
            keytool -printcert -jarfile "$AAB" || true
            exit 1
          fi
          rm -f android/key.properties
          echo "built=true" >> "$GITHUB_OUTPUT"
      - uses: actions/upload-artifact@v7
        if: ${{ steps.aab.outputs.built == 'true' }}
        with:
          name: kelimeki-aab
          path: mobile/app/build/app/outputs/bundle/release/app-release.aab
          if-no-files-found: error
      # Artefaktların indirme bağlantısı OTURUM İSTİYOR ve iOS/iPadOS'ta
      # `.apk` dosya seçicide pasif kalıyor (Safari uzantıyı tanımıyor);
      # Appetize'ın Android tarafı da yalnızca ham `.apk` kabul ediyor —
      # zip'i iOS uygulaması sanıp ayrıştıramıyor. Çözüm: derlemeleri
      # herkese açık DÜZ URL'i olan bir prerelease'e koymak, Appetize'ın
      # "URL'den yükle" seçeneği doğrudan çekebilsin.
      - name: Derlemeleri "mobile-latest" prerelease'ine yükle
        # PR'da atlanır: `mobile-latest` PAYLAŞILAN "en son test derlemesi"
        # (bkz. başlıktaki "YAYINLAMA" notu).
        if: ${{ github.event_name != 'pull_request' }}
        env:
          GH_TOKEN: ${{ github.token }}
        working-directory: mobile/app
        run: |
          cp build/app/outputs/flutter-apk/app-release.apk kelimeki.apk
          gh release view mobile-latest >/dev/null 2>&1 || \
            gh release create mobile-latest --prerelease \
              --target "$GITHUB_SHA" \
              --title "Mobil test derlemesi (otomatik)" \
              --notes "Her mobil derlemede ÜZERİNE YAZILIR — sürüm değil, test derlemesidir. Appetize.io'ya URL ile yüklemek için: aşağıdaki dosyanın bağlantısını kopyala."
          gh release upload mobile-latest kelimeki.apk --clobber
          # `.aab` de AYNI release'e (24 Ağustos 2026). ROADMAP FAZ B 0.A2 onu
          # yalnızca `actions/upload-artifact` ile bırakıyordu; artefakt
          # bağlantısı OTURUM İSTİYOR ve dosyayı ZIP'leyerek veriyor — yani
          # Play Console'a yükleyecek kişi (iPad'den çalışıyor) önce giriş
          # yapıp zip indirip açmak zorundaydı. Bu, `.apk`yı bu release'e
          # taşıyan gerekçenin ta kendisi. Artefakt DURUYOR (CI içi kanıt).
          # Yayınlanan `.aab` bir sır taşımıyor: aynı koddan derlenen `.apk`
          # zaten herkese açık ve upload anahtarı pakete girmiyor.
          if [ "${{ steps.aab.outputs.built }}" = "true" ]; then
            cp build/app/outputs/bundle/release/app-release.aab kelimeki.aab
            gh release upload mobile-latest kelimeki.aab --clobber
          fi
      # Appetize'a otomatik yükleme (7 Ağustos 2026) — iPad Safari'nin dosya
      # seçicisi/sürükle-bırak'ı .apk için günlerce "400"/"silik dosya" ile
      # tıkanıyordu (iOS .apk'nın UTI'sini tanımıyor, çözümü yok). Appetize'ın
      # REST API'si `{"url": ...}` gövdesiyle çağrılırsa dosyayı SUNUCU
      # SUNUCUYA kendisi çekiyor — tarayıcı hiç devreye girmiyor.
      # `public-key` SABİT (aşağıda) — bu, 7 Ağustos'taki keşif koşusunda
      # public-key VERİLMEDEN oluşturulan app'in anahtarı; artık her koşu
      # AYNI app'i günceller, session linki (appURL) BİR DAHA DEĞİŞMEZ.
      # `manageURL` bilerek YAZDIRILMIYOR — Appetize'ın kendi action'ı
      # (maxep/appetize-upload-action) bunu `setSecret` ile maskeliyor, aynı
      # ihtiyatı burada da uyguluyoruz. `publicKey`/`appURL` PAYLAŞILABİLİR
      # (Appetize'ın kendi tasarımı — session linkleri zaten herkese açık).
      - name: "Appetize'a yükle (Android)"
        if: ${{ github.event_name != 'pull_request' }}
        env:
          APPETIZE_API_TOKEN: ${{ secrets.APPETIZE_API_TOKEN }}
        run: |
          if [ -z "$APPETIZE_API_TOKEN" ]; then
            echo "APPETIZE_API_TOKEN secret'ı yok — adım atlanıyor."
            exit 0
          fi
          resp=$(curl -sS -u "${APPETIZE_API_TOKEN}:" \
            -H "Content-Type: application/json" \
            -d "{\"url\":\"https://github.com/${{ github.repository }}/releases/download/mobile-latest/kelimeki.apk\",\"platform\":\"android\",\"note\":\"CI otomatik — ${{ github.sha }}\"}" \
            https://api.appetize.io/v1/apps/oexlhcjxdl6onjr4dewaarnvwa)
          echo "$resp" | jq '{publicKey, appURL}'

  ios:
    name: iOS (imzasız)
    # ⚠ `!cancelled()` ŞART, süs değil: iş `needs: android` taşıyor ve
    # GitHub Actions'ta BAĞIMLILIĞI ATLANAN İŞ DE ATLANIR. Bu olmadan
    # `workflow_dispatch` ile `android: false` + `ios: true` seçmek iOS'u da
    # sessizce atlıyordu — 9 Eylül 2026'da yaşandı (koşu #605: üç iş de
    # `skipped`, hiçbir hata yok, sebep log'da GÖRÜNMÜYOR). `always()`
    # DEĞİL: koşu iptal edildiğinde iOS'un yine de başlamasını istemiyoruz.
    # Android DÜŞERSE iOS yine koşar ve bu doğru — `needs` yalnızca
    # `gh release create` çakışmasını önlemek için var (aşağı bkz.).
    if: ${{ !cancelled() && (github.event_name != 'workflow_dispatch' || inputs.ios) }}
    runs-on: macos-latest
    permissions:
      contents: write
    # Android işi release'i oluşturuyor; ikisi aynı anda `gh release create`
    # çalıştırıp çakışmasın diye iOS onu bekliyor (yükleme birkaç saniye).
    needs: android
    steps:
      - uses: actions/checkout@v7
      # Derleme kimliği — arayüzdeki teşhis satırına gömülür ki bir ekran
      # görüntüsü "hangi commit çalışıyor" sorusunu KENDİ BAŞINA yanıtlasın
      # (bkz. mobile/CLAUDE.md, "Deploy Doğrulaması"). `pull_request`
      # olayında `github.sha` MERGE commit'idir — gerçek head'i kullan,
      # yoksa ekrandaki sha hiçbir dalda bulunmayan bir commit'i gösterir.
      - id: stamp
        run: |
          SHA=$(git rev-parse --short "${{ github.event.pull_request.head.sha || github.sha }}")
          echo "sha=$SHA" >> "$GITHUB_OUTPUT"
          echo "time=$(date -u '+%d.%m %H:%M')" >> "$GITHUB_OUTPUT"
      - uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true
      # `--no-codesign`: sertifika/profil GEREKTİRMEZ, yalnızca derlemenin
      # (pod install dahil) çalıştığını kanıtlar. Apple üyeliği olmadan da
      # koşar. Podfile depoda yok; flutter tooling burada üretir.
      - name: iOS derle
        working-directory: mobile/app
        run: |
          flutter pub get
          # `--build-number`: TestFlight aynı numarayı İKİNCİ kez kabul etmiyor
          # ("bundle version must be higher"). Android'in `.aab` adımı da aynı
          # sayacı kullanıyor, yani iki mağazadaki numaralar hizalı kalıyor.
          flutter build ios --release --no-codesign \
            "--build-number=${{ github.run_number }}" \
            "--dart-define=BUILD_SHA=${{ steps.stamp.outputs.sha }}" \
            "--dart-define=BUILD_TIME=${{ steps.stamp.outputs.time }}" \
            --dart-define=SUPABASE_URL=${{ secrets.SUPABASE_URL }} \
            --dart-define=SUPABASE_ANON_KEY=${{ secrets.SUPABASE_ANON_KEY }}
      # SİMÜLATÖR derlemesi — Appetize.io iOS uygulamasını cihaz `.ipa`'sı
      # olarak DEĞİL simülatör `.app`'i olarak istiyor, ve simülatör
      # derlemeleri İMZA GEREKTİRMİYOR: Apple Developer üyeliği olmadan da
      # iOS'u tarayıcıda test edebiliyoruz.
      #
      # `--debug` zorunlu: Flutter simülatör için release derlemeyi
      # desteklemiyor (AOT yok). Sonuç JIT ile çalışır — biraz yavaş, ama
      # görsel/işlevsel doğrulama için yeterli.
      - name: iOS simülatör derlemesi (Appetize için)
        working-directory: mobile/app
        run: |
          flutter build ios --simulator --debug \
            "--dart-define=BUILD_SHA=${{ steps.stamp.outputs.sha }}" \
            "--dart-define=BUILD_TIME=${{ steps.stamp.outputs.time }}" \
            --dart-define=SUPABASE_URL=${{ secrets.SUPABASE_URL }} \
            --dart-define=SUPABASE_ANON_KEY=${{ secrets.SUPABASE_ANON_KEY }}
          # Appetize yüklemesi ZIP bekliyor; `.app` bir klasör olduğundan
          # sıkıştırmadan yüklenemez. upload-artifact kendi zip'ini
          # ürettiğinden çift zip oluyor — indirince bir kez açman yeterli,
          # içinden Runner.app.zip çıkar (Appetize'a O yüklenir).
          cd build/ios/iphonesimulator
          zip -ry Runner.app.zip Runner.app
      - uses: actions/upload-artifact@v7
        with:
          name: kelimeki-ios-simulator
          path: mobile/app/build/ios/iphonesimulator/Runner.app.zip
          if-no-files-found: error
      - name: Simülatör derlemesini prerelease'e yükle
        if: ${{ github.event_name != 'pull_request' }}
        env:
          GH_TOKEN: ${{ github.token }}
        working-directory: mobile/app
        run: |
          cp build/ios/iphonesimulator/Runner.app.zip kelimeki-ios-simulator.zip
          gh release upload mobile-latest kelimeki-ios-simulator.zip --clobber
      # Appetize'a otomatik yükleme — android işindeki aynı adımla birebir
      # aynı gerekçe/mekanizma. `public-key` sabit: 7 Ağustos'taki keşif
      # koşusunda oluşturulan iOS app'in anahtarı.
      - name: "Appetize'a yükle (iOS)"
        if: ${{ github.event_name != 'pull_request' }}
        env:
          APPETIZE_API_TOKEN: ${{ secrets.APPETIZE_API_TOKEN }}
        run: |
          if [ -z "$APPETIZE_API_TOKEN" ]; then
            echo "APPETIZE_API_TOKEN secret'ı yok — adım atlanıyor."
            exit 0
          fi
          resp=$(curl -sS -u "${APPETIZE_API_TOKEN}:" \
            -H "Content-Type: application/json" \
            -d "{\"url\":\"https://github.com/${{ github.repository }}/releases/download/mobile-latest/kelimeki-ios-simulator.zip\",\"platform\":\"ios\",\"note\":\"CI otomatik — ${{ github.sha }}\"}" \
            https://api.appetize.io/v1/apps/onpdavcakhztlouyedivwrcrdi)
          echo "$resp" | jq '{publicKey, appURL}'
      # ⚠ SIRA BİLİNÇLİ: bu adım Appetize'dan SONRA. Önce konulmuştu ve
      # yanlıştı — imzalama düşerse sonraki adımlar ATLANIR ve bugün çalışan
      # (doğrulanmış) simülatör + Appetize akışı imzalama yüzünden bozulurdu.
      # Yeni ve doğrulanmamış bir adım, çalışan bir adımı asla rehin almamalı.
      # ---- TestFlight: imzalı .ipa (FAZ C 24.2) -----------------------------
      # ⚠ BU ADIM HENÜZ HİÇ KOŞMADI. App Store Connect API anahtarı Apple'ın
      # indirme ucundaki bir arıza yüzünden alınamadı (bkz.
      # marketing/app-store/console-formlari.md § 3). Secret'lar gelene kadar
      # adım kendini atlıyor — yani bugün hiçbir şeyi değiştirmiyor, mevcut
      # imzasız derlemeler ve Appetize akışı olduğu gibi duruyor.
      #
      # KORUMA DESENİ Android'in `.aab` adımının aynısı: `secrets` bağlamı
      # adım düzeyinde `if:`te KULLANILAMAZ (GitHub'ın bağlam tablosunda yok),
      # bu yüzden kontrol script'in İÇİNDE.
      #
      # ⚠ YALNIZCA `main` ve elle tetikleme. Her dal push'unda TestFlight'a
      # yüklemek hem build numarası yakar hem testçilere çöp paket gönderir.
      #
      # ⚠ `flutter build ios` YUKARIDA koştu ve fastlane onun ÜSTÜNE arşiv
      # alıyor — `--dart-define`larla gömülen derleme kimliği ve Supabase
      # anahtarları oradan geliyor. Sıra değişirse imzalı pakette teşhis
      # satırı boş çıkar (`Derleme yerel`) ve sunucuya hiç bağlanmaz.
      - name: TestFlight'a yükle (imzalı .ipa)
        if: ${{ github.ref == 'refs/heads/main' || github.event_name == 'workflow_dispatch' }}
        working-directory: mobile/app
        env:
          APP_STORE_CONNECT_KEY_ID: ${{ secrets.APP_STORE_CONNECT_KEY_ID }}
          APP_STORE_CONNECT_ISSUER_ID: ${{ secrets.APP_STORE_CONNECT_ISSUER_ID }}
          APP_STORE_CONNECT_KEY_P8: ${{ secrets.APP_STORE_CONNECT_KEY_P8 }}
          MATCH_PASSWORD: ${{ secrets.MATCH_PASSWORD }}
          MATCH_GIT_TOKEN: ${{ secrets.MATCH_GIT_TOKEN }}
        run: |
          set -euo pipefail
          if [ -z "${APP_STORE_CONNECT_KEY_P8:-}" ]; then
            echo "APP_STORE_CONNECT_KEY_P8 secret'ı yok — TestFlight adımı atlanıyor."
            exit 0
          fi
          # ⚠ `KEY_P8` bu adımın "yapılandırıldı mı" SENTİNELİ; yanındaki iki
          # değer eksikse adım yine de koşar ve fastlane'in `ENV.fetch`i ham
          # bir Ruby `KeyError`'ı ile düşer — log'da sebebi görünmez.
          # 9 Eylül 2026'da gerçekten yaşandı: `ISSUER_ID` secret listesine
          # hiç girmemişti (öteki üçü vardı) ve eksikliği ancak listeye
          # bakınca anlaşıldı. Bu yüzden ÜÇÜ DE tek tek kontrol ediliyor.
          eksik=""
          [ -z "${APP_STORE_CONNECT_KEY_ID:-}" ] && eksik="$eksik APP_STORE_CONNECT_KEY_ID"
          [ -z "${APP_STORE_CONNECT_ISSUER_ID:-}" ] && eksik="$eksik APP_STORE_CONNECT_ISSUER_ID"
          if [ -n "$eksik" ]; then
            echo "::error::App Store Connect anahtarı EKSİK yapılandırılmış — yok:$eksik (KEY_P8 var). Issuer ID: App Store Connect → Users and Access → Integrations, anahtar listesinin ÜSTÜNDE."
            exit 1
          fi
          if [ -z "${MATCH_GIT_TOKEN:-}" ] || [ -z "${MATCH_PASSWORD:-}" ]; then
            echo "::error::API anahtarı var ama MATCH_GIT_TOKEN/MATCH_PASSWORD yok — imzalama yapılamaz."
            exit 1
          fi
          # Sertifika deposunun URL'i token TAŞIYOR, bu yüzden repoda değil
          # burada kuruluyor (depo PUBLIC). `match` onu Matchfile'dan okuyor.
          export MATCH_GIT_URL="https://x-access-token:${MATCH_GIT_TOKEN}@github.com/alpcapa/kelimeki-certificates.git"
          # ⚠ ANAHTARLIK BURADA AYARLANMIYOR — `Fastfile`'daki `setup_ci`
          # yapıyor. Burada bir zamanlar `MATCH_KEYCHAIN_NAME=kelimeki-ci`
          # yazıyordu ve o anahtarlığı KİMSE OLUŞTURMUYORDU; match
          # "Could not locate the provided keychain" ile düştü (koşu #606,
          # 9 Eylül 2026). Buraya tekrar bir keychain değişkeni EKLEME —
          # setup_ci'nin kendi anahtarlığını ezer ve hata geri gelir.
          export FASTLANE_SKIP_UPDATE_CHECK=1
          export FASTLANE_HIDE_CHANGELOG=1
          bundle install --quiet
          bundle exec fastlane ios beta
      - uses: actions/upload-artifact@v7
        if: ${{ always() && hashFiles('mobile/app/build/ios/ipa/*.ipa') != '' }}
        with:
          name: kelimeki-ipa
          path: mobile/app/build/ios/ipa/*.ipa
          if-no-files-found: ignore

  # Web derlemesi — ÜRÜN HEDEFİ DEĞİL, TEST ORTAMI.
  #
  # NEDEN VAR: geliştirici iPad'den çalışıyor; elinde ne Mac ne Android cihaz
  # var. Tarayıcı içi emülatör (Appetize) ücretsiz katmanda 3 dakikayla
  # sınırlı. (⚠ Bu cümle 8 Eylül 2026'ya kadar üçüncü bir gerekçe daha
  # sayıyordu — "Apple Developer üyeliği askıda" — o gerekçe DÜŞTÜ, üyelik
  # alındı. Kalan ikisi duruyor: Mac yok, Appetize süreli.) Flutter'ın web hedefi
  # aynı Dart kodunu aynı çizim motoruyla (CanvasKit) koşturuyor — yani
  # yerleşim/font/gölge/oyun akışı doğrulaması ücretsiz ve süresiz yapılabilir.
  # Bu iş her mobil push'ta GitHub Pages'e deploy eder, adres sabit kalır.
  #
  # NEYİ KANITLAMAZ: platform davranışı. Paylaş sayfası, dosya sistemi,
  # oturum kalıcılığı, uygulama ikonu, gerçek dokunmatik jestler web'de
  # FARKLI çalışır — bkz. mobile/TESTING.md, "Web derlemesiyle neyi test
  # edemezsin". Depolama da farklı bir arka uç kullanır (IndexedDB üstünde
  # WASM sqlite3, bkz. lib/src/storage/web_db.dart).
  #
  # İKİ DEFA ELLE AÇILMASI GEREKEN AYAR (7 Ağustos 2026'da ikisine de
  # takıldık, ikisi de depo ayarı — kodla çözülemez):
  #   1. Settings → Pages → Source = "GitHub Actions". Yoksa
  #      `configure-pages` adımı düşer.
  #   2. Settings → Environments → github-pages → Deployment branches:
  #      Pages açıldığında GitHub bu ortamı YALNIZCA VARSAYILAN DALA
  #      kısıtlıyor. Port dalından deploy edebilmek için kısıt gevşetilmeli
  #      ("No restriction" ya da `claude/*` kuralı) — aksi halde iş HİÇ ADIM
  #      ÇALIŞMADAN, ~2 saniyede ve LOGSUZ düşer (bu belirti tanıdık
  #      gelmezse saatler yakar).
  web:
    name: Web (GitHub Pages test ortamı)
    # PR'da HİÇ koşmaz: bu işin tek amacı Pages'e deploy etmek ve o site
    # PAYLAŞILAN test ortamı — merge edilmemiş kodla değiştirilemez.
    # (`environment:` iş seviyesinde, koşullu yapılamıyor; ayrıca ortamın
    # dal kısıtı PR ref'inde işi logsuz düşürebilir.) Web'e ÖZGÜ derleme
    # kırılmaları PR'da `test` işindeki "Web derlemesi kontrolü" adımıyla
    # yakalanıyor.
    if: ${{ github.event_name != 'pull_request' && (github.event_name != 'workflow_dispatch' || inputs.web) }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deploy.outputs.page_url }}
    steps:
      - uses: actions/checkout@v7
      # Derleme kimliği — arayüzdeki teşhis satırına gömülür ki bir ekran
      # görüntüsü "hangi commit çalışıyor" sorusunu KENDİ BAŞINA yanıtlasın
      # (bkz. mobile/CLAUDE.md, "Deploy Doğrulaması"). `pull_request`
      # olayında `github.sha` MERGE commit'idir — gerçek head'i kullan,
      # yoksa ekrandaki sha hiçbir dalda bulunmayan bir commit'i gösterir.
      - id: stamp
        run: |
          SHA=$(git rev-parse --short "${{ github.event.pull_request.head.sha || github.sha }}")
          echo "sha=$SHA" >> "$GITHUB_OUTPUT"
          echo "time=$(date -u '+%d.%m %H:%M')" >> "$GITHUB_OUTPUT"
      - uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true
      - uses: actions/configure-pages@v5
      # `--no-web-resources-cdn`: CanvasKit'i gstatic.com yerine kendi
      # adresimizden servis eder — dış bağımlılık yok, kısıtlı ağlarda da
      # açılır. `--base-href`: Pages siteyi /kelimeki/ altında yayınlıyor.
      # `--pwa-strategy=none`: service worker YOK; her açılışta en son
      # derleme gelir (test ortamında bayat önbellek en son isteyeceğimiz şey).
      - name: Web derle
        working-directory: mobile/app
        run: |
          flutter pub get
          flutter build web --release \
            --no-web-resources-cdn \
            --pwa-strategy=none \
            --base-href /kelimeki/ \
            "--dart-define=BUILD_SHA=${{ steps.stamp.outputs.sha }}" \
            "--dart-define=BUILD_TIME=${{ steps.stamp.outputs.time }}" \
            --dart-define=SUPABASE_URL=${{ secrets.SUPABASE_URL }} \
            --dart-define=SUPABASE_ANON_KEY=${{ secrets.SUPABASE_ANON_KEY }}
      # ⚠ Bu üç Pages action'ı (configure-pages / upload-pages-artifact /
      # deploy-pages) 18 Ağustos 2026'daki sürüm yükseltmesinde BİLEREK
      # dokunulmadan bırakıldı: bu iş yalnızca `workflow_dispatch` ile
      # koşuyor, yani bir PR'da DOĞRULANAMAZ. Doğrulanamayan bir major
      # sürüm atlaması, kapatmaya çalıştığı uyarıdan daha büyük bir risk.
      # Yükseltilecekse ayrı bir turda, işi elle tetikleyip yeşil görerek.
      - uses: actions/upload-pages-artifact@v3
        with:
          path: mobile/app/build/web
      - id: deploy
        uses: actions/deploy-pages@v4
