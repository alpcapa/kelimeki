# Bileşen Notları — Oyun Ekranı ve Kabuk

> **Bileşen post-mortem'lerinin oyun ekranı/kabuk cildi — ve üçünün ANA
> girişi.** 26 Ağustos 2026'da bu dosya 183 KB'a çıkıp uyarı bandına
> girdiği için üçe bölündü (kök `CLAUDE.md` → "Doküman Boyutu Bütçesi").
> Hiçbir satır değiştirilmedi, yalnızca taşındı.
>
> | Cilt | İçindekiler |
> |---|---|
> | `components-account.md` | RemainingTilesModal, GameOver, CountBadge, UserMenu, RelationIcons, Auth mesajları, AuthModal, AccountSettingsModal, avatar yükleme |
> | `components-score.md` | ScoreCard, "Sanal Lig" → k-lig rebrand'i, Leaderboard |
> | **`components.md` (bu dosya)** | Setup, PlayerAvatarRow, LandscapeHint, AddToHomeScreen, useAppIconBadge, Board, GameHeader, HelpModal, LogoMark, useModalA11y, TermsModal/PrivacyModal + port dalı teslim dersi |
>
> **Yeni bir bileşen notu** konusuna en yakın cilde yazılır; hiçbirine
> uymuyorsa bu dosyaya.

## Bileşen Notları — oyun ekranı ve kabuk

- **`Setup`** — Oyun başlangıç ekranı. "Nasıl oynanır?" linki burada da bulunur.
  **Misafir giriş uyarısındaki ikinci buton "DEVAM" DEĞİL "OYNA" (18 Ağustos
  2026, kullanıcı bildirdi):** Girişsizken "Oyunu Başlat"a basınca çıkan
  uyarının butonları "Giriş Yap" ve "Devam"dı. Uyarı metni üyeliğin
  faydalarını sayıyor ("istatistikler, k-lig ve arkadaşınla canlı oyun için
  lütfen giriş yapın"), dolayısıyla "Devam" o cümlenin DEVAMI gibi okunup
  *"devam edersem üyeliğe gider"* izlenimi veriyordu — kullanıcının sözleri:
  *"Yazıyı okuyunca devama basmak üyeliğe götürecekmiş gibi düşündürüyor."*
  Yeni etiket ne olacağını söylüyor: misafir olarak oyun başlar. **Flutter
  portu (`setup_screen.dart`, `_showGuestWarning`) AYNI PR'da değişti** —
  iki taraf da "OYNA", biri değişirse öteki de. Davranış hiç değişmedi
  (`doStart` / `_GuestChoice.proceed`); ✕ ve Escape hâlâ ne oyunu başlatıyor
  ne giriş penceresi açıyor. **Duman testi tuzağı:** oyun ekranındaki OYNA
  düğmesi de aynı erişilebilir adı taşıdığından `tests/smoke.spec.ts`
  locator'ı modalın `aria-label="Giriş uyarısı"`ıyla daraltılmak zorunda
  kaldı; negatif eşle iki testin gerçekten bu dala bağlı olduğu doğrulandı.
  **Karşılama sayfasına dönüş girişi web ve mobil portta BİLEREK farklı yerde** (18 Ağustos 2026): web başlıkta bir `<` düğmesi taşırken, yalnızca girişsiz kullanıcıda (bkz. "Karşılama Katmanı — Sertleştirme"), portta bu girişin planlanan karşılığı Setup başlığına DEĞİL hesap menüsüne gelecek — bkz. `mobile/CLAUDE.md`.
  **Girişli kullanıcıda logo altındaki tanıtım metni/linkler kalktı (18 Ağustos 2026, kullanıcı isteği — "Bölüm 1: Girişli/Setup" spesifikasyonu):** Logonun hemen altındaki tanıtım paragrafı ("Kelimeler kurarak bölgeni genişlet…") ve "Nasıl oynanır?" satırı artık **yalnızca misafir (girişsiz) kullanıcıda** görünüyor — girişli kullanıcı logonun ardından doğrudan "OYUN TİPİ" başlığını görüyor. Misafirin ekranı (ölçümler dahil) **byte-for-byte değişmedi**: logo→"OYUN TİPİ" arası hâlâ 152.50px (390px genişlikte) / 136.50px (834px genişlikte), footer hâlâ yalnızca "Kullanım Koşulları · Gizlilik Politikası" + "© Kelimeki" (aşağı bkz.). **Girişli için ölçülen değer: logo→"OYUN TİPİ" arası TAM 20.00px** — kapsayıcının kendi `flex flex-col gap-5`i bunu otomatik veriyor; çocukları (paragraf+link satırı) kaldırmak dışında **hiçbir telafi edici `mt-*`/`gap` EKLENMEDİ** — bir sonraki oturum "boşluk az/çok" diye bir marj eklemeye kalkışırsa bu, kapsayıcının kendi boşluğunu ikinci kez sayan bir hata olur.
  **"Arkadaşınla paylaş" iki ayrı küçük metin linkinden footer'ın KENDİ üçüncü maddesine indirgendi (18 Ağustos 2026, aynı gün ikinci ve üçüncü tur).** Öncesinde iki AYRI giriş noktası vardı: misafirde logonun altında "Nasıl oynanır? · **Arkadaşınla paylaş**" satırı, girişlide ise footer'da (Kullanım Koşulları/Gizlilik Politikası'nın yanında) küçük bir "**Paylaş**" metin linki — ikisi de aynı `handleShare`'i çağırıyordu ama farklı yerlerde, farklı koşullarda (yalnızca biri veya öteki) görünüyordu. Kullanıcı önce ikisini kaldırıp *"tanıtımdaki gibi alta paylaş koyalım"* dedi — ilk denemede bu, `Landing.tsx`'teki `Oyna`/`Giris` CTA çiftiyle aynı stilde (`btn-raised-neutral`, tam genişlikte) BÜYÜK bir buton olarak yapıldı, ama kullanıcı bunu reddetti: *"Yanlış anladın, buton istemedim. Tanıtım footerındakinin aynısını istedim."* — yani "tanıtımdaki gibi" ifadesi Landing'in CTA butonlarına değil, Landing'in FOOTER'INDAKİ (Kullanım Koşulları/Gizlilik Politikası'nın kullandığı) küçük metin-linki stiline işaret ediyordu. **Nihai hâl:** "Paylaş" footer'ın hukuki linkler satırına ÜÇÜNCÜ madde olarak eklendi — `Kullanım Koşulları · Gizlilik Politikası · Paylaş`, üçü de BİREBİR AYNI className (`hover:underline active:opacity-70 transition-opacity`, satırın ortak `text-[10px] font-mono text-muted`'ı miras alıyor). `handleShare` girişten bağımsız çalıştığından (`?ref=arkadas` linkini oluşturup native paylaşım/clipboard'a düşüyor, `user`'a hiç bakmıyor) `user &&` gibi bir koşula BAĞLANMADI — misafir de girişli de aynı satırı görüyor.
  **Ders:** "tanıtımdaki gibi" (X sayfasındaki gibi) bir istek geldiğinde hangi ÖĞEYE benzetildiği (buton mu, küçük link mi) referans sayfada birebir aramadan/karşılaştırmadan tahmin etmek yanlış sonuç üretti — Landing'in footer'ında o ana kadar zaten "Paylaş" diye bir öğe hiç yoktu (yalnızca hukuki linkler + © Kelimeki), yani "aynısı" ifadesi bir ELEMAN eşleşmesi değil bir STİL eşleşmesiydi; bu ayrım ilk seferde atlandı.
  **İkon geri geldi, İKİ tarafa birden (18 Ağustos 2026, aynı gün dördüncü tur):** Kullanıcı bu sefer *"Daha önce tanıtım sayfasına universal paylaş ikonlu paylaş linki koymuştuk, uçmuş. Şimdi sen setup'a ekledin, başında ikon yok. İki tarafa da ikonlu şekilde koy."* dedi. **Git geçmişi Landing.tsx'in HİÇBİR ZAMAN "Paylaş" içermediğini gösteriyor** — kullanıcının hatırladığı ikonlu link muhtemelen Setup'ın ESKİ (bu oturumdan önceki) footer'ındaki `<ShareIcon size={12} />`'li "· Paylaş" linkiydi, o link bu oturumun ikinci turunda (büyük buton denemesi) kazara ikonsuz bırakılmıştı. Kullanıcının niyetini teknik doğruluğa tercih ederek — "uçan ikonu geri getir VE iki tarafa da koy" — üç değişiklik birden yapıldı: (1) `ShareIcon` Setup'ın "Paylaş" linkine GERİ eklendi; (2) Landing.tsx'e YENİ bir "Paylaş" linki eklendi (`data-kelimeki-paylas`, aynı ikon, footer'ın hukuki linkler satırına dördüncü/üçüncü sıradaki madde); (3) `?ref=arkadas` linkini üreten mantık `src/utils/shareLink.ts`'e (`buildShareUrl`/`shareKelimekiLink`) ÇIKARILDI — Setup.tsx (React, `handleShare`) VE `main.tsx`'in yeni `paylasiKur()`'u (React DEĞİL, Landing'in hiçbir olay handler'ı olamayacağından — bkz. Landing.tsx dosya başlığı) AYNI fonksiyonu çağırıyor, iki ayrı paylaşım implementasyonu `?ref=arkadas` etiketini sessizce ayrıştırma riskini taşırdı. Landing'in "Paylaş"ı butonun İÇİNDE ayrı bir `<span data-kelimeki-paylas-metin>` taşıyor — `paylasiKur()` kopyalama sonrası yalnızca o span'in metnini 2 saniyeliğine "Link kopyalandı!" yapıyor (Setup'ın `shareCopied` state'iyle aynı geri bildirim), düğmenin TAMAMINA `textContent` yazmak yanındaki `ShareIcon` SVG'sini de silerdi.
  **Mobil porta BİLİNÇLİ OLARAK taşınmadı** — port hâlâ kendi footer'ındaki (ikonsuz) küçük "Paylaş" linkini taşıyor (bkz. "Mobil port AYNI PR'da birebir uygulandı", `mobile/CLAUDE.md` Parça 110); web'in bu turdaki `ShareIcon` eklemesi porta hiç işlenmedi — istenirse porta da taşınabilir, bu turda kapsam dışı bırakıldı. Landing'in "Paylaş"ı zaten web'e özgü (mobil portun karşılama katmanı yok, bkz. "Karşılama Katmanı — web'e özgü, bilinçli ayrışma").
  **Footer'ın altına "© Kelimeki" (18 Ağustos 2026, kullanıcı isteği: "setup altındaki footer'ın altına 'c Kelimeki' (tanıtımdaki gibi) olsun"):** Hukuki linkler satırının hemen altına, `Landing.tsx`'in "Son çağrı" footer'ındaki AYNI stil (`font-mono text-[10px] text-muted`) ve AYNI metinle (`© Kelimeki`) bir satır eklendi. İki satır artık ortak bir `flex flex-col items-center gap-1` sarmalayıcının içinde — bu sarmalayıcı dış kabın kendi `gap-5`'ine tek bir çocuk olarak katılıyor, yani üstteki içerikle arasındaki mesafe DEĞİŞMEDİ, yalnızca hukuki linkler ile telif satırı arasına küçük (4px) bir boşluk eklendi. **Ölçüldü** (derlenmiş CSS + Chromium): iki satır arası 4.0px, ekran görüntüsüyle Landing'in footer'ıyla görsel olarak eşleştiği doğrulandı. Girişli/girişsiz her iki dalda da aynı satır görünür — footer'ın kendi içeriği hiçbir zaman `user`'a bağlı olmadı ("Paylaş" de dahil, bkz. yukarıdaki madde — üçü de aynı satırın koşulsuz üyesi). **Bu satır bir dönem "mobil porta BİLİNÇLİ OLARAK taşınmadı, `setup_screen.dart`'ta hiçbir telif satırı yok" diyordu — 19 Ağustos 2026'da GEÇERSİZLEŞTİ:** port o gün kendi footer'ına aynı satırı aldı (bkz. `mobile/CLAUDE.md`, Parça 117). **Ve aynı gün orada bir hiza hatası çıktı (kullanıcı cihazda bildirdi): telif satırı SOLA yapışıyordu.** Sebep web'de hiç karşılığı olmayan bir Flutter ayrıntısı — kapsayıcı `Column` `CrossAxisAlignment.stretch` olduğundan `Text` tam genişliği kaplıyor ve `textAlign` verilmezse varsayılan `start`a düşüyor; web'de aynı satırı ortalayan şey sarmalayıcının `items-center`ı. **Mevcut test bunu göremiyordu ve bu ders kayda değer:** footer testi telif satırının hukuki satırın ALTINDA olduğunu ölçüyordu (dikey sıra DOĞRUYDU) ve `stretch` altında GEOMETRİK bir ortalama kontrolü de işe yaramazdı — kutu zaten tam genişlikte, sola kaçan şey kutunun İÇİNDEKİ glyph'ler. Regresyon testi bu yüzden boyamayı belirleyen özelliğin kendisini (`textAlign`) ölçüyor. **İki footer artık gerçekten aynı; biri değişirse öteki de değişmeli.**
  **Boşluk 4 → 12px (19 Ağustos 2026, kullanıcı: "Setup en alttaki '© Kelimeki' ile onun üstündeki footer grubu arasında tanıtım sayfasındaki kadar boşluk olsun"):** Yukarıdaki 4.0px ölçümü doğruydu ama Landing'le EŞİT DEĞİLDİ — orada iki satırı ayıran şey sarmalayıcının kendi `gap-1`i değil, "Son çağrı" `<section>`ının `gap-3`ü (12px). Setup'ın sarmalayıcısı `gap-1` → **`gap-3`** yapıldı. **Ölçüldü** (derlenmiş CSS + Chromium, gerçek Setup ekranı ve gerçek `dist` katmanı): 360/390/834'te Setup **12.00** px, Landing de üç genişlikte **12.00** px — artık birebir; yatay taşma 0, footer'ın üstündeki içerikle arası (dış kabın `gap-5`i) DEĞİŞMEDİ. İki footer'ın eşitliği bilinçli bir değişmez: biri değişirse öteki de değişmeli.
  **Oyuncu satırındaki parantezli puan KALDIRILDI (17 Ağustos 2026, kullanıcı isteği: *"Yz ile oyun açılışlarında kişi yanında parantez içinde yazan puan gerçek puan değil. Ama düzeltme, puanı oradan kaldır."*):** Girişli kullanıcının 1. koltuk satırı adının yanında `Ironman (92)` gibi bir sayı gösteriyordu; sayı `fetchPlayerStats(2)` + `fetchPlayerStats(4)` sonuçlarının `total_score` toplamıydı ve **11 Ağustos 2026'daki k-lig ödül sistemi bu toplamı gerçek puandan KOPARDI** — ödüller yalnızca `player_stats_overall.bonus_points`ta yaşıyor, mod bazlı `player_stats` satırlarına BÖLÜNMÜYOR (bkz. "k-lig Ödül & Rütbe Sistemi": *"Genel lig puanı = 2 kişilik + 4 kişilik … + ödül puanı"*). Ölçülen fark: satır **92**, hesap menüsündeki k-lig satırı **97** (aradaki 5, alınmış bir eşik ödülü). **Kullanıcı düzeltmeyi değil KALDIRMAYI seçti** — doğru sayı zaten iki dokunuş ötede (hesap menüsü k-lig satırı, Skor Kartı, k-lig listesi) ve kurulum ekranındaki bir oyuncu satırında puan göstermenin işlevi yok. `accountTotalScore` state'i ve onu dolduran `useEffect` de silindi (tek tüketicisiydi). **Flutter portunda bu gösterim HİÇ YOKTU** (`setup_screen.dart` yalnızca ad + `PlayerBadge` çiziyor), yani bu aynı zamanda sessiz bir ayrışmayı kapatıyor — geri eklenirse İKİ tarafa birden ve `player_stats_overall` üzerinden eklenmeli.
  **GİRİŞ/avatar satırı ile logo arası 0px (17 Ağustos 2026; 13 Ağustos'ta 4'e çekilmişti) —** kullanıcı Blok 6 görsel turunda *"az boşluk bize alt kısımda daha fazla yer kazandırır"* diyerek 4'ü de kaldırdı. **İKİ taraf BİRLİKTE indi** (web `-mt-6`, portta `SizedBox` silindi): kullanıcı web'in boşluğunu porttakinden hafif fazla görüyordu ama KODDA ikisi de 4'tü ve fark bu ortamdan üretilemedi (port render edilemiyor, web'in girişli hâli gerçek oturum ister) — yalnız web'i indirmek, kodda eşit olan iki sayıyı gerçekten ayrıştırırdı. **Merge sonrası cihazda ~3px'lik bir fark HÂLÂ görünüyor ve bu KAPATILDI (kullanıcı kararı) —** kovalamak 3px için orantısız bulundu. Elenen hipotezler, tekrar denenmesin diye: (1) **GİRİŞ butonunun avatardan uzun olması** — kullanıcı karşılaştırmayı ikisi de GİRİŞLİYKEN yaptı, o yüzden ilgisiz; (2) **`LogoMark`'ın viewBox iç boşluğu** — iki taraf da aynı `viewBox`ı (`218.82 91.84 984.57 318.16`) ve aynı `height/vbHeight` ölçeğini kullanıyor, vektör birebir aynı; (3) **avatar kutusu** — web'de `width/height:32` + `border-box`, portta `size:32` + border içeride, ikisi de 32. Portun boşluğunun 0 olduğu `setup_screen_test`in ölçen testiyle kanıtlı, web'de de aritmetik 0. Kalan adaylar (alt-piksel yuvarlama, DPR, kaydırma kabı) ancak portu render ederek ayrıştırılabilir. Aşağıdaki 13 Ağustos notu tarihsel kayıt olarak duruyor:
  **(13 Ağustos 2026, kullanıcı isteği: "App'de logoyla avatar satırı arası ideal, web'de ekstra boşluk var"):** Bu sayı iki ayrı kutunun toplamı — `App.tsx`'in kurulum dalındaki hesap satırı (`px-3.5 pt-3`) ile Setup'ın kendi kabı (`px-4 py-6`) AYRI kutular, aradaki mesafeyi kabın `py-6`sı (24) ile logo bloğunun NEGATİF üst margin'i belirliyor. O margin `-mt-3` (−12 → arası 12) iken **`-mt-5`e** (−20 → arası 4) çekildi; hesap satırının kendi `pt-3`ü (üstte 12) DEĞİŞMEDİ. **Bu, portun sayısının web'e uygulandığı bilinçli bir istisna** (mobil `setup_screen.dart` baştan 4 kullanıyordu) — iki taraf da 4, biri değişirse öteki de değişmeli; mobil tarafta bunu `setup_screen_test.dart`'ın "GİRİŞ satırının üstü/altı web ile aynı (12/4)" testi sabitliyor. Ölçüm derlenmiş CSS + Chromium ile iki viewport'ta (420/1000) yapıldı: 12.0 / 4.0. **Negatif margin'i gözden kaçırma** — yalnızca `py-6`ya bakan bir hesap 24 çıkarır. **"Neden Üye Olmalıyım?" kutusu** (31 Temmuz 2026): yalnızca misafir (girişsiz) kullanıcı için, "Yapay Zeka ile" sekmesinin kurulum formunun altında (hem 2 hem 4 oyunculu alt sekmede — kutunun bulunduğu `else` dalı `count`'tan bağımsız olduğundan otomatik ikisinde de çıkıyor) çek işaretli 6 maddelik bir üyelik faydaları listesi + "Giriş Yap / Kayıt Ol" butonu (`AuthModal`'ı açar) gösterir: Canlı oyun, Skor takibi/Sanal Lig, aynı anda birden fazla YZ oyunu, cihazlar arası kesintisiz devam, oyun geçmişini saklama/beğenme/paylaşma, arkadaş ekleme. Girişli kullanıcı "+ Yeni Yapay Zeka Oyunu" formunu açtığında (`creatingLocal`) aynı `else` dalına düşse de `!user` koşuluyla kutu gizli kalır — "Arkadaşınla" sekmesinde de hiç render edilmez.
  **Aynı gün ikinci değişiklik — misafirin "Devam Eden Oyun" satırındaki yanıltıcı "teslim sayılacak" metni düzeltildi, kutu buraya da eklendi:** Kullanıcı, misafirken (girişsiz) açılan bir oyunun kalan-süre etiketinde "... sonra teslim sayılacak" yazdığını, oysa bunun "silinecek" olması gerektiğini fark etti — haklıydı: `SavedGameRow`'un `willSurrender` parametresi (`remainingTime`, `Setup.tsx`) `turnCount>=2`'ye bakıyordu, bu eşik yalnızca GİRİŞLİ kullanıcının bulut kaydı (`cloudSaves`) için doğru anlamı taşıyor (hesabına GERÇEKTEN/hemen -2 uygulanacak) — misafirin hesabı olmadığından süre dolunca kesin/garanti olan tek şey kaydın localStorage'dan silinmesi, turnCount>=2 olsa bile bir teslim kaydı yalnızca kişi AYNI cihazda 7 gün içinde SONRADAN üye olursa devreye giren koşullu bir iç detay (bkz. "Terk edilen oyunun otomatik temizliği"). Misafirin `SavedGameRow`'una artık `willSurrender={false}` sabit geçiliyor — etiket her zaman "... sonra silinecek" gösteriyor. Aynı düzeltmeyle birlikte altındaki açıklama paragrafı da (önceden "teslim olmuş kabul edilirsin; ayrıca lig puanından 2 puan düşülür" gibi aynı yanlış vaadi taşıyordu) şuna değiştirildi: "Bu oyun 7 gün boyunca cihazınızın hafızasında saklanır ve bir sonraki gelişinizde devam edilebilir. Üye değilseniz bu oyunu bitirmeden yeni oyun açamazsınız." — ve kullanıcı isteğiyle `MembershipPerksBox` artık yalnızca boş kurulum formunun altında değil, misafirin bu "Devam Eden Oyun" görünümünde de (aynı paragrafın hemen altında) gösteriliyor. Girişli kullanıcının bulut kayıtları (`cloudSaves.map`) bu değişiklikten etkilenmedi — `willSurrender={save.state.turnCount >= 2}` aynen duruyor, çünkü onlar için "teslim sayılacak" gerçek ve anında bir sonuç. **Aynı gün üçüncü değişiklik — paragraf ile kutu arasına boşluk:** kullanıcı ikisinin birbirine çok yapışık göründüğünü belirtti; `MembershipPerksBox` artık opsiyonel bir `className` prop'u kabul ediyor, bu kullanım yerinde `className="mt-2"` ile üst boşluk ekleniyor (kutunun form-altı diğer kullanımı, `!user && <MembershipPerksBox ... />`, `className` vermediğinden eskisi gibi kalıyor).
- **"Devam eden oyun" kartı — İKİ SEKME AYRIŞMIŞTI (2 Eylül 2026, kullanıcı iki ekran görüntüsüyle bildirdi; `1.0.5`, `Derleme 4a0a29b`):** Setup'ın "DEVAM EDEN OYUNLAR" listesi **Yapay Zeka** (`Setup.tsx` → `SavedGameRow`) ve **Arkadaşınla** (`LiveGamesTab.tsx` → `GameRow`) sekmelerinde farklı diziliyordu: YZ'de kalan süre kendi ALT satırındayken, Canlı'da durum etiketiyle AYNI sağ sütundaydı ve "X açtı" yazısına biniyordu. (6 Eylül 2026: "X açtı" satırı KALKTI — kurucu zaten `slots[0]`, ilk avatar — yerine iki kartta da avatarların altına koltuk sırasıyla PUAN SATIRI geldi, `utils/scoreLine.ts`; "Son Oynananlar"da tarih avatarların üstüne çıktı, bitiş puanları altına indi. Canlı puanları `fetchOnlineGameGlances` — eski `fetchOnlineGameDeadlines`in `players`ı da seçen hâli — tek istekle getiriyor.)
  **Kök sebep düzenin yanlış olması DEĞİL, doğrusunun ULAŞILAMAZ olması:** doğru şekil aynı günün erken bir turunda (#408) YZ kartında kurulmuştu ama portta `_DevamEdenGovde` olarak `setup_screen.dart`'ın İÇİNDE, yani **private** doğmuştu; Canlı kartı o turda dokunulmadan kaldı. Web'de de aynı ayrışma vardı (`flex-col` ↔ `flex items-center`) — yani bu bir port hatası değil, iki platformda birden tekrarlanan tek bir yapı hatasıydı.
  **Ölçüm (320 px, portun ikizinde):** durum ve süre tek bir sağ sütunda toplandığında sütunun enini SÜRE belirliyor — "SIRA SENDE" **89,6 px**, süre satırı **194,3 px**. Yani sol sütunu daraltan etiket değil süredir, üstelik yazı ölçeği 1,0'da bile.
  **Çözüm:** gövde + tipografi ortak kaynağa çıkarıldı (`mobile/app/lib/src/ui/devam_eden_govde.dart`; web'de iki bileşen aynı sınıf dizisini paylaşıyor). Aynı turda **YZ kartındaki "Sıra: X" alt satırı KALDIRILDI** (kullanıcı isteği) — yanındaki `SIRA SENDE` ile aynı şeyi söylüyordu; ⚠ Canlı kartının aynı yerdeki **"X açtı" satırı buna BENZEMEZ ve KALIR** (kimin açtığını söylüyor, sıra bilgisi değil). Durum etiketinin puntosu 13 → **15** (kullanıcı isteği), üçgen/nokta ölçüsü ona çapalı olduğundan 8×9/9×9 → 9×10/10×10 (web SVG'si ve Dart path'i birlikte; `relation_icon_parity_test.dart` senkronu zorluyor).
  ⚠ **Testte yakalanan sessiz tuzak:** sıkışmayı ölçen iddia sol sütunu `find.ancestor(of: PlayerAvatarRow, matching: Column)` ile buluyordu. "Sıra: X" kalkınca sol taraf tek bir `PlayerAvatarRow`a indi — kendi eni SABİT (36 px) — ve bulucu DIŞ sütuna sıçrayıp kartın tamamını ölçerdi: test düşmez, **sessizce anlamsızlaşırdı**. Sol alan artık ortak gövdede anahtarlı (`kDevamEdenSolKey`). Bir düzeni ölçen test, o düzenin İÇERİĞİ değişince yeniden okunmalı.

- **`PlayerAvatarRow`** (`src/components/PlayerAvatarRow.tsx`) — Bir oyunun katılımcılarını yan yana (hafif üst üste binen, varsayılan 20px) avatarlar olarak gösterir; oyun kartlarında **"N Kişilik Oyun" KALIN BAŞLIK SATIRININ yerine** geçer (avatar sayısı zaten oyuncu sayısını gösterdiğinden metin bilgi kaybettirmiyor). Bu yüzden çağıranlar oyuncuların TAMAMINI (kendileri dahil) geçer — yalnızca rakipleri göstermek 4 kişilik bir oyunda oyunun kaç kişilik olduğunu kaybettirirdi. Üç görünüm var: normal üye (`Avatar` — fotoğraf ya da baş harf), **YZ koltuğu** (`isAi` — baş harf yerine `bg-void`/`border-border` çember içinde 🤖; `LiveGameCreateForm`/`PendingGameCard`'daki davet robot avatarıyla birebir aynı görsel dil, çünkü "YZ" baş harfi üretmek onu gerçek bir üye gibi gösteriyordu) ve **misafir koltuğu** (`isGuest`).
  **`isGuest` — YZ oyunlarında da Canlı'daki gibi avatar (3 Ağustos 2026, kullanıcı isteği):** Bileşen önce yalnızca Canlı oyun kartlarında (`LiveGamesTab`) kullanılıyordu; yerel/YZ tarafındaki karşılığı olan `Setup`'un "Devam Eden Oyun(lar)" satırları (`SavedGameRow`) hâlâ düz `"N Kişilik Oyun"` metnini gösteriyordu — kullanıcı bu tutarsızlığı fark edip iki tarafın aynı görünmesini istedi. `SavedGameRow` artık `title: string` yerine `players: AvatarRowPlayer[]` alıyor ve `Setup`'taki ortak `savedGameAvatars()` yardımcısıyla dolduruluyor: YZ koltukları robot avatarı, insan koltuğu ise kişinin avatarı (2 kişilikte 1 insan + 1 robot, 4 kişilikte 1 insan + 3 robot).
  **`isGuest` — misafir koltuğu için "?" (bu satırda ÖNLEYİCİ, `RecentGamesSection`'da GERÇEK bir düzeltme):** İnsan koltuğunun adını doğrudan `Avatar`'a geçirmek misafirde yanlış sonuç verir — `GameState`'e literal `GUEST_PLAYER_NAME` (`'Misafir'`, `src/game/constants.ts`) gömüldüğünden (bkz. `doStart`) baş harfler "MI" çıkıp misafiri gerçek bir üye gibi gösterir. `isGuest` bu koltuğa `name=""` geçirerek `Avatar`'ın boş isimde ürettiği **"?"** yedeğini seçtiriyor (`initials()`'ın `if (!n) return '?'` dalı — yeni bir görsel icat edilmedi, var olan yedek bilinçli olarak seçildi). **Dikkat, tarihsel doğruluk:** `SavedGameRow`'da bu HİÇBİR ZAMAN kullanıcının gördüğü bir hata olmadı — o satırda avatar diye bir şey yoktu, düz `"N Kişilik Oyun"` metni vardı; yani burada "MI" düzeltilen değil, avatarı eklerken düşülmesi ÖNLENEN bir tuzak. "MI" gerçekten görünen tek yer `RecentGamesSection`'dı (aşağı bkz.).
  **`RecentGamesSection`'daki gerçek "MI" (aynı gün, ikinci adım):** "Son Oynadıklarım" bitmiş oyunları `games.players` anlık görüntüsünden render ediyor ve o snapshot ismi KALICI olarak donduruyor (`buildGameRecord`, `r.player.name`) — misafirken oynanıp bitirilen, sonra aynı cihazda üye olunca `flushPendingGames` ile hesaba taşınan bir yerel oyun listede "MI" baş harfli avatarla görünüyordu. Snapshot bilerek `user_id` taşımadığından (girişli herkese açık, bkz. `GamePlayerSnapshot`) tespitin tek yolu isim karşılaştırması: `!p.is_ai && !g.online_game_id && p.name === GUEST_PLAYER_NAME`. `online_game_id` kontrolü yanlış pozitifi daraltıyor — Canlı'da misafir koltuk YOK, oradaki herkes kayıtlı, dolayısıyla takma adı gerçekten "Misafir" olan bir üye (nickname benzersiz olduğundan en fazla BİR kişi olabilir) Canlı kartlarında baş harflerini korur; yalnızca kendi yerel oyunlarında "?" görür (kozmetik, kabul edildi). Yazan (`doStart`) ile okuyanın (`RecentGamesSection`) sessizce ayrışmaması için iki taraf da tek bir `GUEST_PLAYER_NAME` sabitini kullanıyor.
  **"?" punto düzeltmesi (aynı gün, kullanıcı testinden sonra):** `Avatar`'ın baş harf yedeği `size * 0.4` kullanıyordu — bu oran İKİ harfe göre ayarlı, daireye yatayda ancak öyle sığıyorlar. Tek karakterlik yedekte bu kısıt yok ve 0.4 optik olarak zayıf kalıyordu: 20px'lik satırda "?" 8px iken hemen yanındaki robot avatarı 11px (`0.55`) olduğundan "?" belirgin şekilde küçük duruyordu (kullanıcı bildirdi). `Avatar` artık yedek metni tek karakterse aynı `0.55`'e çekiyor (`text.length === 1 ? 0.55 : 0.4`) — iki harfli baş harfler, yani `Avatar`'ın kullanım yerlerinin ezici çoğunluğu, hiç etkilenmiyor. Oran tahminle değil ölçülerek seçildi (mevcut robot avatarının oranıyla eşitlendi) ve 4x yakın çekim ekran görüntüsüyle gözle doğrulandı — bkz. `CountBadge`'in "ölçmeden 'optik olarak kayık' teşhisi koyma" dersi.
  **"Login olunca '?' gerçek avatara döner" ek bir taşıma/migration GEREKTİRMİYOR:** `Player`/`GameState` avatar taşımadığından (`src/game/types.ts`'te böyle bir alan yok) fotoğraf dondurulmuş state'ten değil O ANKİ `profile`'dan okunuyor. Yerel oyunda insan koltuğu her zaman 0. koltuktur ve her zaman bu cihazdaki kişidir, misafirin kaydı da giriş sonrası zaten hesaba taşınıyor (bkz. "Devam eden oyunun kalıcılığı", `migratingSavedGameRef`) — yani kişi giriş yapınca satır `cloudSaves` dalına geçip `profile?.avatar_url` ile render ediliyor, "?" kendiliğinden o kişinin avatarına dönüyor. Girişli ama profili henüz yüklenmemiş kısa anda `isGuest` yine `false` geçildiğinden "?" değil kendi adının baş harfleri görünür (geriye "?"e düşmez). Doğrulama: misafir olarak 2 ve 4 kişilik birer YZ oyunu başlatılıp Setup'a dönülerek Playwright ile uçtan uca teyit edildi (eski `"Kişilik Oyun"` metninin hiç kalmadığı, "?" + doğru sayıda robot avatarının göründüğü); girişli varyant (fotoğraf/baş harf) `LiveGamesTab`'ın zaten üretimde çalışan AYNI kod yolu olduğundan ayrıca test edilmedi. **`RecentGamesSection`'daki misafir tespiti uçtan uca DOĞRULANMADI** — gerçek Supabase oturumu + misafir kuyruğunun (`flushPendingGames`) hesaba aktarılması gerekiyor, bu ortamda kurulamadı; yalnızca `tsc` ve kod okumasıyla teyit edildi (render tarafı, yani `isGuest` → "?", Setup satırında zaten uçtan uca doğrulanmış AYNI kod yolu).
- **`LandscapeHint`** (`src/components/LandscapeHint.tsx`) — Yatay modda dokunmatik cihazlarda (`pointer: coarse`) gösterilen, kapatılabilir bir öneri banner'ı ("Dikey konumda daha iyi bir deneyim yaşarsınız"). Setup ve oyun ekranlarının ikisinde de mount edilir; `sessionStorage`'a (`kelimeki_landscape_hint_dismissed`) kapatılınca bir daha o sekmede çıkmaz. 25 Temmuz 2026'ya kadar burada `index.html`/`index.css`'teki `#landscape-block` ile `#root`'u tamamen gizleyen SERT bir blok vardı (yalnızca telefon genişlikleriyle sınırlıydı, `max-width: 1024px`). Gerçek bir iPad'de test edilirken iki ayrı deneme başarısız oldu: önce `pointer: coarse` kullanıldı ama iPadOS trackpad'li bir klavye TAKILIYKEN de birincil işaretleyiciyi kasıtlı olarak `coarse` raporluyor (dokunmatik-uyumluluk için); sonra `any-pointer: fine`'a geçildi ama bu sefer trackpad hiç TAKILI OLMASA bile `true` döndü (muhtemelen Apple Pencil desteği yüzünden — bkz. WebKit bug 212580/209292, `any-pointer`/`pointer` iPad'de güvenilmez). Yani CSS media query'leriyle "gerçekten trackpad/mouse var mı" sorusunu iPad'de kesin çözmek mümkün değil. Çözüm: sert bloğu tamamen kaldırıp App'i hiç gizlemeyen bu düşük riskli banner'a geçmek — trackpad'i olan biri yanlışlıkla görse bile tek tıkla kapatıp oynamaya devam eder, hiçbir zaman kilitlenmez.
- **`AddToHomeScreen`** (`src/components/AddToHomeScreen.tsx`) — `isStandaloneDisplay()` (`src/utils/visitTracking.ts` — iOS'ta `navigator.standalone`, diğerlerinde `display-mode: standalone` media query'si) `false` iken 1.2sn sonra çıkan, kapatılabilir bir "Ana ekrana ekle" banner'ı. **1 Ağustos 2026'ya kadar** kapatma `sessionStorage`'a (`kelimeki_a2hs_dismissed_session`) yazılıyordu — kullanıcı, uygulamayı zaten ana ekrana eklemiş olsa bile bir e-posta linkinden (her zaman normal tarayıcı sekmesinde açılır, standalone DEĞİLDİR — bir sayfanın "bu cihazda PWA zaten kurulu mu" diye sorabileceği güvenilir/platformlar-arası bir API yok) her tıklayışında YENİ bir sekme/oturum açıldığından banner'ı defalarca görüyordu. `DISMISSED_KEY` artık `localStorage`'a (`kelimeki_a2hs_dismissed`) yazılıyor — bir kez kapatan kullanıcı o cihazda bir daha hiç görmüyor (e-posta linkinden gelse bile). **Platform kısıtı (düzeltilemez):** iOS'ta bir e-posta linkinin doğrudan ana ekrandaki kurulu PWA'yı açması mümkün değil — Universal Links yalnızca App Store'dan kurulan native uygulamalar için çalışıyor, "Ana Ekrana Ekle" ile kurulan bir web-app bu mekanizmaya hiç giremiyor; Android'de Chrome'un WebAPK'sı link-capturing ile bunu kısmen ("Şununla aç" seçeneği, kullanıcı "varsayılan yap" derse otomatikleşir) sağlayabiliyor ama garantili değil.
- **`useAppIconBadge`** (`src/hooks/useAppIconBadge.ts`, 1 Ağustos 2026) — "Ana Ekrana Ekle" ile kurulan PWA'nın kendi İKONU üzerinde (masaüstü/dock/görev çubuğu) tarayıcının **Badge API**'siyle (`navigator.setAppBadge`/`clearAppBadge`) kırmızı yuvarlak/beyaz sayı rozeti gösterir — uygulama hiç açılmadan da görünür. Kullanıcı isteğiyle Setup'taki sekme rozetleriyle/UserMenu'deki "Arkadaşlar" rozetiyle AYNI üç sinyalin toplamı: bekleyen arkadaşlık isteği + bekleyen Canlı davet/sırası gelen aktif Canlı oyun (Setup'taki `liveActionCount` ile birebir aynı hesap, hook kendi içinde bağımsız olarak tekrarlıyor) + devam eden Yapay Zeka oyunu sayısı (`localSaveCount`, App.tsx'in zaten sahip olduğu `cloudSaves`/`savedGame`'den — hook'a parametre olarak geçiliyor, yeniden hesaplanmıyor). App.tsx seviyesinde (hangi ekran açık olursa olsun, Setup'a hiç girilmese bile) çağrılıyor. Badge API iOS'ta desteklenmiyor (Android/masaüstü Chrome/Edge'de çalışıyor) — `'setAppBadge' in navigator` ile feature-detect edilip yoksa sessizce hiçbir şey yapılmıyor; API çağrısı da bir `try/catch`'e sarılı (bazı tarayıcı/izin durumlarında reddedebiliyor). Uygulama tamamen kapalıyken (arka planda bile çalışmıyorken) bu rozeti canlı güncelleyecek bir mekanizma yok — bunun için Push API + Service Worker gerekir, bilinçli olarak kapsam dışı; rozet yalnızca uygulama açıkken/arka plan sekmesindeyken (Realtime + focus/visibility/online olayları + 10 dakikalık periyodik tazeleme, `OnlineGameScreen`'deki aynı desen) güncelleniyor. Bekleyen arkadaşlık isteği sayısı için ayrı bir Realtime aboneliği yok (`friend_requests` tablosu publication'a hiç eklenmedi, `UserMenu`'deki mevcut davranışla tutarlı) — yalnızca mount + foreground olaylarında tazeleniyor.
  **Aynı gün ikinci değişiklik — Setup'a dönüşte Canlı/arkadaşlık kısmı da tazeleniyor (tutarlılık):** İlk sürümde yalnızca `localSaveCount` (App.tsx'in `cloudSaves`'i zaten Setup'a her dönüşte tazelemesi sayesinde) güncel kalıyordu — Canlı oyun/arkadaşlık kısmı yalnızca Realtime (`online_games`/`game_invites` tabloları) + focus/visibility/online olaylarına bağlıydı, ve bir hamle oynamak yalnızca `online_game_states`'i değiştirdiğinden (bu tabloya abone değiliz) tek başına rozeti tetiklemiyordu — kullanıcı iki bekleyen Canlı oyunu da oynayıp uygulamadan hiç çıkmasa rozet "2" göstermeye devam ediyordu, gerçek duruma ancak yukarıdaki tetikleyicilerden biri (ör. 10 dakika geçmesi) gerçekleşince dönüyordu. Kullanıcı bunu fark edip iki kısmın (`localSaveCount` vs Canlı/arkadaşlık) tutarsız davrandığını belirtince, hook'a üçüncü bir parametre (`isOnSetupScreen`) eklendi — App.tsx'te `state.phase === 'setup' && !onlineGame` olarak hesaplanıyor (Canlı oyun ekranı `onlineGame` adında AYRI bir state olduğundan, yalnızca `state.phase`'i izlemek Canlı oyundan Setup'a dönüşü kaçırırdı — bu ayrım kod yazılırken fark edilip düzeltildi). Bu değer `true` olduğunda (Setup'a her dönüşte) hook `refreshAllRef` üzerinden Canlı/arkadaşlık kısmını da yeniden çeker — artık Setup'a dönüş, `localSaveCount` VE Canlı/arkadaşlık kısmı için ortak bir tazeleme anı.
- **`Board` alt şeridindeki "Çevrimdışı" göstergesi (14 Ağustos 2026):** `!online` iken (bkz. `useOnlineStatus`) şeridin sağında kırmızı bir "Çevrimdışı" yazısı çıkar. Puntosu/aralığı, şeritteki KARDEŞ kontrollerle (Hamleler · Mesajlaşma · Nasıl Oynanır?) birebir aynı olmak zorunda (`text-[12px] font-mono font-bold tracking-[0.5px]`, yalnız rengi farklı) — önceden `text-[8px]`ti ve kullanıcı cihaz testinde "belli olmuyor" diye bildirdi: tam da çevrimdışıyken okunması gereken tek gösterge, şeridin en küçük yazısıydı. Bir kardeş kontrolün puntosu değişirse bu da değişmeli. **AMA asıl hata puntoda değildi (aynı gün, kullanıcı İKİ KEZ bildirdi):** `useOnlineStatus` YALNIZCA `online`/`offline` OLAYLARINI dinliyordu; uçak modunu açmak için Kontrol Merkezi'ne çıkıldığında sayfa askıya alındığından olay JS'e hiç ulaşmıyor ve durum sonsuza dek bayat `true` kalıyordu — yani rozet tam da gerektiği anda hiç çıkmıyordu. Hook artık öne dönüşte (`visibilitychange` + `focus`) `navigator.onLine`'ı YENİDEN OKUYOR; olay TÜRÜNE değil o anki değere bakan tek bir `sync` kullanılıyor. **Bu, bu projede kaçırılan olayın kalıcı kayba dönüştüğü ÜÇÜNCÜ yer** (sohbet Realtime'ı ve bulut senkronu aynı çareyi almıştı) — olay tabanlı yeni bir durum eklerken ilk soru "kaçırılırsa ne olur?" olmalı. Gerçek tarayıcıda ölçülerek doğrulandı ve `tests/smoke.spec.ts`'e kalıcı bir regresyon testi eklendi (olay ateşlemeden `navigator.onLine`'ı false yapıp öne dönüşü taklit ediyor; düzeltme geri alınınca GERÇEKTEN düşüyor). **Cihazda da doğrulandı** (14 Ağustos 2026, ana ekrana eklenmiş PWA, uçak modu Kontrol Merkezi'nden açılarak) — rozet artık gerektiği anda çıkıyor. **Flutter portu 14 Ağustos 2026'da aynı göstergeyi aldı** (`mobile/CLAUDE.md`, Parça 97): orada `Board.tsx` gibi bir hook olmadığından bağlantı durumu `BoardWidget`'a enjekte ediliyor (`onlineStatus`) ve İKİ oyun ekranı da onu geçmek zorunda — biri atlanırsa uyarı yalnızca bir ekranda çıkar. **İki taraf artık aynı sinyali kullanıyor** (`navigator.onLine`; portta `connectivity_plus` web'de zaten ona bakıyor), yani metin/punto/renk değişirse ikisi birlikte değişmeli.
- **`Board` — "Buradan başla" balonu (26 Ağustos 2026, kullanıcı isteği):** Kapalı testte insanların kuralı değil **ilk hamleyi nereye yapacaklarını** bulamadıkları görüldü. `HomeMark` zaten duruyordu ama ne olduğunu söyleyen bir şey yoktu — tanıtımda okunan cümle, tahtaya bakarken hatırlanmıyor. Sırası gelen İNSAN oyuncunun ev karesinin yanında, tahtanın içine doğru uzanan küçük bir balon çıkar; kuyruğu kareye bakar. **Görünme koşulunun üç parçası da bilinçli:** (1) tahtada tek taş yok, (2) bu turda konmuş TASLAK taş da yok — oyuncu oynamaya başladıysa ipucu görevini bitirmiştir ve balon taslağın üstünü kapatmamalı, (3) sıra bir insanda (YZ düşünürken "buradan başla" demek anlamsız). **Kalıcı bir "görüldü" bayrağı YOK** (`onboarding.ts`'e dördüncü bir anahtar eklenmedi): koşul kendi kendini sınırlıyor ve bir bayrak, cihaz değiştiren ya da uzun aradan sonra dönen oyuncuyu ipuçsuz bırakırdı. **Konum yüzdeyle ifade EDİLEMEZ ve bu ölçülerek bulundu:** ızgarada 12 adet 3px boşluk var, yani bir hücre `100%/13` değil `(100% - 36px)/13`; yüzde yaklaşımı balonu dikeyde ~9px kaydırıyordu (Chromium, 656px ızgara). Konum artık `calc` ile tam geometriden (`GRID_GAP`/`CELL_W`). Aynı fark köşe numarası/X2 filigranlarında GÖRÜNMÜYOR çünkü onlar 4×4/5×5 blokları kabaca kaplıyor — tek bir HÜCREYE hizalanan her yeni katman bu formülü kullanmalı. **Regresyon (`tests/smoke.spec.ts`):** iddia kendi formülüne değil GERÇEK `[data-cell="0,0"]` kutusuna karşı; balonun sol kenarı o karenin sağından tam bir ızgara boşluğu sonra başlamalı ve dikey merkezleri **0.75px** içinde olmalı. Tolerans sıkı ve bu bilinçli — ilk sürüm 1.5px'ti ve **yanlış (yüzde tabanlı) sürümü de geçiriyordu**; negatif eş kurulurken yakalandı. Ayrıca ilk taş konunca balonun kaybolduğu da ölçülüyor. Flutter portu aynı gün birebir aldı (`mobile/docs/parca-log.md`, Parça 145) — metin ya da koşul değişirse ikisi birlikte değişmeli.
- **`Board` — hamle puanı rozeti taşları kapatıyordu (29 Ağustos 2026, kapalı testteki kullanıcılar bildirdi):** "Oyna"dan önce çıkan yeşil/kırmızı `+N` rozeti (`buildBadge`) kelimenin en üst-sol hücresine `translate(-35%,-35%)` ile oturuyor ve dar telefonlarda ilk taşın harfini örtüyordu. **Ölçüldü, tahmin edilmedi** (384px genişlik → hücre 25.2px; gerçek `Board` çıktısı `dist/index.html`'den çıkarılıp Chromium'da ölçüldü): rozet **30.7px = 1.22 HÜCRE**, yani bir hücreden geniş. Dolgu `3px 6px` → **`1.5px 3px`** yapıldı: **24.7px = 0.98 hücre**, artık tek hücrenin içinde kalıyor. **Rakamın PUNTOSU bilinçli olarak DEĞİŞMEDİ** (kullanıcı kararı, üç seçenek yan yana çizilip karşılaştırıldı): hamle puanı oyuncunun "Oyna"ya basmadan önce baktığı tek sayı, küçültmek şikayeti çözerken başka bir şeyi bozardı.
  **AYRIŞMA KAPANDI — WEB porta getirildi (29 Ağustos 2026, kullanıcı kararı):** Punto/font bir tur boyunca ayrıktı — web `fontSize: clamp(8px,2vw,11px)` + `font-mono` (Space Mono), port sabit `11` + tema sans'ı (SpaceGrotesk); aynı rozet portta %19 geniş çiziliyordu ve şikayetin asıl kaynağı buydu. Bu, projede harf puntosunda birebir yaşanmış olan "web'de fluid olan değer portta sabit kalmış" sınıfının aynısı (bkz. `mobile/CLAUDE.md`, Parça 24) — **ve "akışkan" taraf aslında hiç akmıyordu:** clamp'in `2vw`si 11px'e ancak 550px'te ulaşıyor, yani her telefonda 8px'e kırpılıyordu, fark sabit %19'du.

  **Yön bilerek seçildi:** puntoyu KÜÇÜLTEN seçenek bir tur önce zaten reddedilmişti (hamle puanı "Oyna"dan önce bakılan tek sayı), yani portu web'e çekmek o kararı geri alırdı. Bunun yerine web sabitlendi: `fontSize: '11px'` + `font-sans`. Port DEĞİŞMEDİ.

  **ÖLÇÜLDÜ** (gerçek oyun + Chromium, taslak taş konup rozet metni değiştirilerek; hücre = tahta genişliği / 13), `"+35"` ile: 384px'te (hücre 27,7px) `20,7px = 0,75 hücre` → **`26,1px = 0,94 hücre`**; 320px'te (hücre 22,8px) `20,7 = 0,91` → **`26,1 = 1,15`**.

  ⚠ **320px'te iki haneli bir puan tek hücreyi AŞIYOR — bilerek kabul edildi:** port zaten sabit 11 çizdiğinden 320px'lik bir telefonda bugün de aynı taşmayı yaşıyor; bu değişiklik web'i portun davranışına getiriyor, yeni bir sorun üretmiyor. Şikayet gelirse çözüm İKİ tarafta birden uygulanmalı — tek taraflı bir düzeltme ayrışmayı geri getirir.
- **`Board`** — Her oyuncunun genişleyen bölgesinin tam dış hattı oyuncu rengiyle çizilir (`buildOutline` + `computeAllTerritories`); boş hücreler de kendi bölgesine dahilse tonlanır.
  **Filigran puntoları KUTUYA SIĞDIRILMAZ, ekran genişliğine bağlıdır** — köşe rakamı `clamp(80px, 32vw, 220px)`, merkez "X2" `clamp(60px, 24vw, 165px)` (ikisi de `font-mono` + `leading-none`), merkez hücrenin "X3" etiketi `clamp(7px, 1.9vw, 12px)`. Ölçülen değerler (derlenmiş CSS + Chromium): 390px'te 124.8 / 93.6 / 7.41, 834px ve üstünde tavan olan 220 / 165 / 12. **Flutter portu bu ÜÇ formülü `fluidSize` ile birebir taşıyor** (`mobile/app/lib/src/ui/game/board_widget.dart`, 17 Ağustos 2026 — o güne kadar `FittedBox` ile kutuyu dolduruyordu ve yazı tipi de yanlıştı; bkz. `mobile/CLAUDE.md`, Parça 106); biri değişirse öteki de değişmeli — **21 Ağustos 2026'dan beri TESTLİ** — `mobile/app/test/layout_parity_test.dart` web kaynağını okuyup port sabitiyle karşılaştırıyor (bkz. `mobile/CLAUDE.md`, Parça 127).
  **Filigranların KATMAN SIRASI (z-index) da bir sözleşme:** hücre arka planları → **filigran** (z-auto) → **taşlar** (`relative z-[5]`, `boardTile`/`placedTile` dallarında) → **dış hatlar + puan rozeti** (`z-10`). Yani filigran boş hücrelerin üstünde görünür ama taşın ALTINDA kalır — taşa `z-[5]` veren satırın kendi yorumu da bunu söylüyor. Bu sıra Flutter'da otomatik DEĞİL (z-index yok, sıra boyama sırasıdır): port 17 Ağustos 2026'ya kadar filigranı ızgaradan sonra çizip taşların üstüne bindiriyordu, kullanıcı iki ekranı yan yana koyup bildirdi (bkz. `mobile/CLAUDE.md`, Parça 107). Taşların `z-[5]`i kaldırılırsa ya da yeni bir katman araya girerse İKİ tarafın da kontrol edilmesi gerekir.
  **Bulunan hata (4 Ağustos 2026) — henüz onaylanmamış (Oyna'ya basılmamış) taşlar herkese aynı renkte görünüyordu:** Kullanıcı, Canlı oyunda (sıra kendisinde olsun ya da olmasın — bkz. "sıra kendisinde değilken taş yerleştirip egzersiz yapabilme", Canlı Oyun — Faz 3) tahtaya koyduğu deneme taşlarının her zaman 2. oyuncunun (kırmızı) renginde göründüğünü bildirdi. Kök sebep: onaylanmış tahta taşları (`boardTile` dalı) rengi doğru şekilde `colorOf(boardTile.owner)` ile taşın GERÇEK sahibinden okuyordu, ama hemen altındaki henüz onaylanmamış taş dalı (`placedTile`) bunu hiç yapmıyor, tüm bekleyen taşları tek bir `currentColor` (`state.current`'a, yani O AN SIRASI GELEN oyuncuya göre hesaplanan renk) ile boyuyordu — `tile.owner` (PLACE_TILE zaten doğru atıyordu, bkz. `gameReducer.ts`) hiç okunmuyordu. Kendi sırasında oynarken bu görünmüyordu çünkü `state.current` zaten oynayanın kendisiydi (tesadüfen doğru); off-turn pratikte ise `state.current` rakibe ait kaldığından taşlar rakibin renginde çiziliyordu. **Düzeltme:** `placedTile` dalı da `boardTile` ile aynı deseni izleyip `colorOf(placedTile.owner)` kullanıyor (yalnızca owner hiç yoksa `currentColor`'a düşüyor).
- **`GameHeader` skor kutuları** — sırası gelen oyuncunun çerçevesi 2px, diğerleri 1px (28 Temmuz 2026, birkaç adımda son hâlini aldı). Sırasıyla: (1) önce `boxShadow: inset` 2.5px/1.5px idi — kullanıcı geri bildirimiyle diğerleri 0.75px'e düşürüldü, ama alt piksel `inset` kalınlıkları kenarlar arasında asimetrik render ediyordu (sol/üst kenarlar sağ/alttan kalın); `ScoreBoxRow`taki (`GameHistoryModal.tsx`) aynı dersle tutarlı şekilde gerçek bir CSS `border`a (2px/1px) geçildi. (2) Yalnızca çerçeve kalınlığı yeterince ayrışmadığından sırası gelen kutuya `transform: scale` + `filter: drop-shadow` ile bir "pop" efekti denendi — ama bu, kutuyu üst şeridin (`overflow-x-auto` — CSS'in bir eksen `auto` ise diğerini de `auto` sayma kuralı yüzünden dikeyde de kırpan, `UserMenu`'nün eski dropdown sorununa bkz.) sınırları dışına taşırıp KESİYORDU, üstelik parıltının yayılımı komşu kutuya kadar uzanıyordu. Nihai çözüm: `transform`/`filter` tamamen kaldırıldı; `shadow-raised` class'ının katmanları (className'inkini ezdiği için) inline tekrarlanıp üzerine `BOX_GAP`'in (min 6px) altında kalacak kadar dar (4px bulanıklık, yayılım yok) bir renkli parıltı katmanı eklendi — artık ne kesiliyor ne komşuya taşıyor. (3) **31 Temmuz 2026'nın "çamur gibi duruyor" geri bildirimiyle bu parıltı da kaldırılıp gerçek bir `border`a (2px aktif/0.5px pasif) dönüldü** — ama bu, 1 Ağustos 2026'da kullanıcı ekran görüntüsüyle bulunan yeni bir hataya yol açtı: Tailwind'in `box-sizing:border-box`'ı altında `border` genişliği kutunun İÇ içerik alanından düşüldüğünden, YZ'nin dar kutusunda (max 43px) aktif/pasif arasındaki 1.5px'lik fark (her kenarda) 3 haneli bir skoru sırası AI'dayken (border kalınken) "1…" diye kırpılmaya zorluyor, sıra insana geçip border incelince (genişlik geri açılınca) kendiliğinden düzeliyordu — kullanıcı bunu "YZ oynarken skor kutusu kısa süre böyle görünüp sonra normale dönüyor" diye tarif etti. **Düzeltme:** `border` yerine layout'a hiç dokunmayan `outline` (+ kutunun tam kenarına oturacak negatif `outlineOffset`) kullanılıyor — aynı 2px/0.5px görünümü koruyor ama içerik genişliği artık aktif/pasif durumdan tamamen bağımsız, kırpma imkansız hale geldi.
  **Teslim gösterimi netleştirildi (6 Ağustos 2026, Flutter portundaki GameHeader çalışması sırasında kullanıcı kararı):** Öncesinde teslim olan oyuncunun kutusunda "Teslim" etiket puntosuyla (`LABEL_FONT_SIZE`, 6-8px) yazılıyor ve kutu skor satırı kısa kaldığından diğerlerinden ~10px kısa oluyordu. Artık yeni bir `TESLIM_FONT_SIZE` (aynı 375/465 uç noktalı sistem, 11-16px — skor puntosu kullanılamıyor çünkü 6 karakterlik TESLİM insan kutusuna sığmıyor, Space Mono ~0.6em/karakter hesabıyla türetildi) + `lineHeight: SCORE_FONT_SIZE` ile puan alanını dolduruyor ve kutu diğerleriyle BİREBİR aynı boyda kalıyor. %45 soluklaştırma (`opacity: 0.45`) bilinçli olarak DURUYOR — kutu tasarımı aynı, yalnızca solgun. Flutter portu (`mobile/app/.../game_header.dart`) aynı kararı FittedBox/SizedBox ile uyguluyor, iki taraf aynı görünüyor (bkz. `mobile/CLAUDE.md`). **Karşılaştırılacak bir SAYI çifti yok** (web akıcı clamp, port FittedBox) — bu yüzden 21 Ağustos 2026'da sayının kendisi değil ALTINDAKİ DEĞİŞMEZ kilitlendi: teslim satırı skor satırıyla aynı yükseklikte kalmalı; web bunu `lineHeight: SCORE_FONT_SIZE`, port `SizedBox(height: scoreFontSize)` ile sağlıyor ve `layout_parity_test.dart` ikisini de kaynakta doğruluyor.
  **Canlı oyunda skor kutuları tıklanabilir (3 Ağustos 2026):** Kullanıcı isteğiyle, Canlı bir oyunda header'daki bir skor kutusuna dokunmak o oyuncunun `PlayerScoreCard`'ını açıyor (k-lig/beğenenler/arkadaşlar listelerinden açılanla aynı bileşen). `GameHeader`'a eklenen opsiyonel `onPlayerClick?: (index: number) => void` prop'u verilmezse kutular eskisi gibi düz `div` kalıyor — yani **yerel/YZ oyun ekranı (`App.tsx`) hiç etkilenmiyor**, prop'u geçmiyor. YZ koltukları için ayrı bir "hangi indeksler tıklanabilir" prop'una gerek olmadı: `Player.isAI` zaten state'te, `GameHeader` kutuyu yalnızca `onPlayerClick && !p.isAI` iken `button`a çeviriyor. Kimlik `state.players`'tan DEĞİL `game.slots`'tan alınıyor (`OnlineGameScreen.handlePlayerBoxClick`) — online state'in public kopyası yalnızca görünen adı taşır, `user_id` taşımaz; `PlayerSummary` de `likerToPlayerSummary` (`GameHistoryModal`) ile aynı desende dolduruluyor (sunucu `list_my_online_games`'te adı zaten kısa kimlik kuralıyla hesapladığından `display_name`'e yazılıyor, ad/soyad boş). **`div`→`button` dönüşümünün kutu geometrisini bozmadığı ölçülerek doğrulandı** (74×31.5 ve 43×31.5, iki etikette de birebir aynı) — Tailwind preflight `button`'ın varsayılan border/background/font'unu sıfırladığından yukarıdaki 1 Ağustos genişlik-kayması sınıfındaki bir hata tekrarlamıyor.
- **`GameHeader` — son skor kutusunun sağ kenarı KAYBOLUYORDU; sebep şeridin KIRPMASI (30 Ağustos 2026, `outlineOffset` −0.5 → −1):** Bir kullanıcı iPhone'da bildirdi: *"header'daki YZ kartının sağ kenarı kesiliyor. Web'de, Ipad'de, mobile app'de düzgün duruyor, sadece iphone'da gördüm."*
  - **KÖK SEBEP — `outline` kutunun DIŞINA taşıyor ve şerit onu kırpıyor.** Skor kutuları `overflow-x-auto` taşıyan bir kaydırma şeridinin içinde ve **şeridin sağ kenarı son kutunun sağ kenarıyla TAM OLARAK çakışıyor** (ölçüldü: `serit.right - son.right === 0`, içerik boyutlu şerit). Dolayısıyla dışarı taşan her şey orada kesilir. Pasif çerçeve `outline: 0.5px` + `outlineOffset: -0.5px` yazılmıştı; bu ikisi birlikte çerçeveyi tam kutunun içine oturtur — **ama tarayıcılar 0.5px'lik outline genişliğini 1px'e YUVARLIYOR ve offset'i yuvarlamıyor.** Sonuç: 1px'lik çerçevenin yarısı dışarı taşıyor ve sağda kırpılıyor.
  - **ÖLÇÜLDÜ — tek satır değil, DİKEY PROFİL** (Chromium `deviceScaleFactor: 3`, son kutunun sol ve sağ kenarındaki kırmızı piksel sayısı):

    | y | Sol kenar | Sağ kenar |
    |---|---|---|
    | 34 (köşe) | 4 px | 4 px |
    | **46–100 (düz kısım)** | **3 px** | **0 px** |
    | 112 (köşe) | 4 px | 4 px |

    Yani sağ kenar kutunun TÜM DÜZ KISMINDA yok, yalnızca yuvarlatılmış köşelerde var — orada eğri içeri kıvrıldığı için kırpma sınırının içinde kalıyor.
  - **DÜZELTME: `outlineOffset` = −genişlik.** Aktif kutu bunu zaten yapıyordu (−2/2, hiç sorun yaşamadı); pasifte kural bozulmuştu. Genişlik açıkça `1px`, offset `-1` → çerçeve tamamen içeride, kırpılacak bir şey yok. Düzeltme sonrası profil: düz kısımda iki kenar da **3 px**, simetrik.
  - ⚠ **BU HATA TEK BİR TARAMA SATIRIYLA GÖRÜLMEZ.** İlk teşhis turunda y=108 taranmış, o satır tesadüfen bir KÖŞEYE denk gelmiş ve "Chromium bu hatayı üretemiyor, sebep iOS'un alt-piksel rasterleştirmesi" diye yanlış bir sonuca varılmıştı. Bir kenarın çizilip çizilmediğini ölçerken **kenarın tamamını tara**, tek satır örnekleme yapma.
  - **Port ikizi (`game_header.dart`) ETKİLENMİYOR ama kalınlık hizalandı:** Flutter'da `Border.all` `foregroundDecoration` içinde tanım gereği kutunun İÇİNE çizilir, yani taşma/kırpma sınıfı orada hiç doğmuyor. Kalınlık yine de `active ? 2 : 1` yapıldı — tarayıcılar zaten 1 çizdiğinden bu, iki tarafı gerçekte görünen değere hizalıyor.
  - **Bu bulguya varmadan önce ÜÇ yanlış teşhis yapıldı** (görünmez bir sarmalayıcı kutusu "boyalı kutu" sanıldı; sonra sayfa taşması arandı; sonra alt-piksel fazı suçlandı). Dersleri: `docs/decisions/touch-ux-bugs.md` → "`UserMenu` de `.tap-expand`e geçti".

- **`GameHeader` — logonun altında "← Geri" (21 Ağustos 2026, kullanıcı isteği: *"Bazı kullanıcılar oyundan setup'a dönüşü bulamıyor"*):** Logo BAŞTAN BERİ Setup'a dönüyordu (`handleLogoClick`) — eksik olan davranış değil **GÖRÜNÜRLÜKTÜ**. Etiket logoyla AYNI `<button>`ın içinde, yani dokunma alanı ikisini birden kapsıyor (kullanıcı şartı: *"Logo alanı da dahil basıldığında"*).
  - **Prop YOK, koşulsuz çiziliyor.** `GameHeader`ın TEK kullanım yeri iki oyun ekranı (`App.tsx` + `OnlineGameScreen.tsx`) ve ikisi de bunu istiyor; opsiyonel bir bayrak bu kod tabanının en sık hatasını (o iki dosyanın sessizce ayrışması) davet ederdi.
  - **Etiket AKIŞIN DIŞINDA (`absolute`) ve bu, kullanıcının açık şartı:** ilk sürüm akışta duruyordu, header'ı **+28px** büyütüp tahtayı aşağı itiyordu ve `items-center` yüzünden skor kutularını logodan **14px** aşağı kaydırıyordu — reddedildi (*"Olmamış. Header'ı bozmadan…"*). Yerini logo altında ZATEN var olan 16px'lik boşluk (header `py-2.5` + Board `pt-1.5`) açıyor; **fazladan dolgu EKLENMEDİ**, header ve tahta bir piksel bile oynamıyor.
  - **`left-0` — tahtanın sol kenarına hizalı (kullanıcı isteği):** logo butonunun sol kenarı header'ın `px-3`ü, Board kabı da aynı `px-3`ü kullanıyor, yani ikisi de 12px. Ortalanmış bir etiket logonun genişliğine göre kayardı. **⚠ Board'un yatay dolgusu değişirse bu hiza sessizce bozulur — ikisi birlikte değişmeli.**
  - **İnce ve KOYU:** 11px, normal ağırlık, `text-text` (#1B2430). İlk deneme kalın + accent'ti ve reddedildi — bu bir aksiyon düğmesi değil, var olan bir dokunuşun sessiz etiketi. Gri (`text-muted`) ve siyah varyantlar yan yana render edilip kullanıcı **siyahı seçti**. Yeni punto/renk icat edilmedi.
  - **`exitDisabled` için ayrı stil GEREKMİYOR** — etiket butonun içinde olduğundan butonun `disabled:opacity-40`ı onu da soluyor.
  - **ÖLÇÜLDÜ** (derlenmiş CSS + Chromium, 390px, öncesi/sonrası): başlık yüksekliği **49.33 → 49.33**, tahta kartının üstü **55.33 → 55.33**, skor kutusu merkezi ile logo merkezi **24.66 / 24.66**, etiketin sol kenarı **12** = tahtanın sol kenarı **12** (fark **0**), punto 11 / ağırlık 400 / renk `rgb(27,36,48)`, yatay taşma **0**. Etiketin altı ile tahta kartı arası 2px — çakışma yok.
  - **Regresyon:** `tests/smoke.spec.ts` 21 → **22 test** (etiket görünür + logo butonunun İÇİNDE + dokununca Setup'a döner). **Negatif eş, ikisi de ayrı ayrı düşürüldü:** `<span>` kaldırılınca ve etiket butonun DIŞINA taşınınca test GERÇEKTEN düşüyor. **Test tuzağı:** Setup'ın başlığı DOM'da `Oyun Tipi`, ekranda büyük harf görünmesi CSS `uppercase`inden geliyor — `getByText('OYUN TİPİ')` HİÇ eşleşmez (bu kod tabanında kayıtlı tuzak, "Oyna"/"Nasıl Oynanır?" vakasının kardeşi).
  - **Flutter portu AYNI PR'da (`mobile/CLAUDE.md`, Parça 125) ve BİR NOKTADA bilinçli olarak ayrışıyor:** Flutter'da kutusunun DIŞINA taşan bir çocuk dokunuş ALMAZ (`RenderBox.hitTest` önce `size.contains`e bakar), yani web'in "absolute + tıklanabilir" birleşimi birebir taşınamıyor. Portta etiket LOGONUN kendi kutusuna çapalı (`Stack` + `Clip.none`), yani konum/boşluk/hiza weble aynı ve header yine hiç büyümüyor — **tek sapma etiketin portta TIKLANABİLİR OLMAMASI** (kaçış yolu logo, etiket onu gösteren ipucu). **İlk deneme CI'da düştü ve gerçek bir hata buldu:** etiket header'ın dış Stack'ine konup konumu stack'in üstünden hesaplanınca, Row logodan uzun olduğunda (360px'te 48px) logo ortalanıp aşağı kayıyor ve etiket onun ÜSTÜNE biniyordu.

- **`HelpModal`** — Oyun kuralları sayfası. Türkçe, kapsamlı.
  **Hızlı Başlangıç'ta üç madde yeniden yazıldı (19 Ağustos 2026, kullanıcı isteği, İKİ platformda birden):** (1) `"…mevcut harflere (rakiplerinki dahil) bağlanmalıdır."` → **`"Yeni kelimeler tahtadaki mevcut harflere bağlanmalıdır. (Senin veya rakibinin)"`** — parantez cümlenin ortasından sonuna alındı, okuma akışı kesilmiyor; (2) `"puanlarını ikiye, üçe katlar"` → **`"ikiye veya üçe katlar"`** (virgül "hem ikiye hem üçe" gibi okunuyordu, oysa çarpanlar birleşmiyor — bkz. "Merkez bonus bölgesi"); (3) TDK maddesine **`(Birkaç istisna dışında)`** eklendi — "Sözlük" bölümü zaten "TDK sözlüğünde olmayan ama bulmacalarda sık kullanılan bazı kelimeler eklenmiştir" diyordu, hızlı başlangıç bunu gizliyordu. **Yazım:** kullanıcı "bir kaç" yazdı, metne TDK yazımıyla (bitişik) **"Birkaç"** girildi — tam da TDK sözlüğünü işaret eden cümlede ayrık yazmak tutarsız olurdu. Portun `help_modal.dart`'ı AYNI PR'da güncellendi (kural metinleri birebir kopyadır); `help_text_parity_test.dart` yalnızca bölüm başlıklarını/madde ikonlarını karşılaştırdığından bu tür bir CÜMLE değişimini yakalamaz — port elle senkronlanmak zorunda.
  **Detaylı Kurallar'da bir cümle netleştirildi (aynı gün, kullanıcı isteği):** "Genel Bakış"ın son cümlesi `"Hamlen herhangi bir oyuncunun bölgesine temas etmiyorsa…"` → **`"Hamlen herhangi bir rakip oyuncunun kelimesine değse bile, bölge teması yoksa puan paylaşımı olmaz ve tüm puan sana kalır."`** Ayrım motorla birebir uyuşuyor ve ÖLÇÜLDÜ değil OKUNDU (`computeInvasionSplit`, `src/utils/validator.ts`): vergi kararı YALNIZCA `computeAllTerritories`'in döndürdüğü bölge kümelerine bakıyor, tahtadaki taşa değil — yani hiçbir bölgeye ait olmayan bir rakip taşına (ör. rakibin kendi zincirine bağlanmamış izole taşı, bkz. "Genişleyen bölge") bitişik oynamak vergi doğurmaz. Eski cümle bunu söylemiyordu ve oyuncular "rakibin harfine değdim, vergi ödedim" sanabiliyordu. Port `help_modal.dart` aynı PR'da güncellendi.
  **"Rütbeler ve Ödüller" bölümü (12 Ağustos 2026, kullanıcı isteği: "rank olayını nasıl oynanır alt kısma ekleyebiliriz"):** Denetimde gerçek bir boşluk çıktı — k-lig bölümü yalnızca puanın nasıl KAZANILDIĞINI anlatıyordu; -2 cezasından, ödül eşiklerinden ve rütbe kademelerinden hiç söz etmiyordu, yani sistemin kullanıcıya görünen tüm ödül/rütbe katmanı hiçbir yerde belgelenmiyordu ("Skor Kartı ve Puanlama"nın sonuna -2'nin iki kaynağı — Canlı 48 saat, yerel 7 gün — ayrıca eklendi). **Tablo ELLE YAZILMIYOR, `RANK_TIERS`ten (`src/utils/leagueRank.ts`) render ediliyor** — eşik/ödül değişirse bu ekran kendiliğinden takip eder; dördüncü bir elle senkron kopya açmak (zaten SQL↔TS↔Dart üç kopya var) bu sistemin en bilinen kırılganlığını büyütürdü. Flutter portu aynı gün aynı bölümü aldı (`mobile/CLAUDE.md`, Parça 66) ve o da kendi `kRankTiers`inden çiziyor — iki ekranın METNİ birebir aynı (kural metinleri web'den kopyalanır, özetlenmez), yalnızca kaynak liste platforma özgü.
- **`LogoMark`** (`src/components/LogoMark.tsx`) — Uygulama logosu ("kelimeki" + alt çizgi swash'ı), `GameHeader` ve `Setup`'ta kullanılır. 23 Temmuz 2026'ya kadar bir web fontuyla (Caveat) canlı metin olarak render ediliyordu; her açılışta yedek fonttan gerçek fonta görünür bir sıçrama (FOUT) yaşanıyordu — üstelik uygulama sık deploy edildiğinden ve PWA service worker'ı her deploy sonrası arka planda güncelleyip sayfayı yeniden yüklediğinden, bu sıçrama tek seferlik değil her deploy sonrası ilk açılışta tekrar tekrar yaşanan bir sorundu (bkz. "Font Yükleme Stratejisi", aşağıda). Çözüm: `scripts/generate-logo-paths.mjs` (opentype.js ile) Caveat Bold'un "kelimeki" glyph'lerini build-time'da statik SVG `<path>` verisine çevirip bu dosyayı üretir — `LogoMark` artık hiçbir fonta veya ağ isteğine bağlı değil, React ağacıyla birlikte anında render olur, FOUT imkansız. Elle düzenlenmemeli, `npm run generate-logo-paths` ile yeniden üretilir (component gövdesi de dahil tüm dosyayı bu script yazıyor — değişiklik LogoMark.tsx'e değil script'e yapılıp yeniden üretilmeli). 6 Ağustos 2026'dan beri aynı script Flutter portunun logosunu da (`mobile/app/lib/src/ui/game/logo_mark_data.dart`, bkz. `mobile/CLAUDE.md`) aynı path verisinden üretiyor — logo değişirse tek komut iki tarafı birden günceller. `height` prop'u sabit bir piksel (`Setup`'ta `52` gibi) ya da akıcı bir CSS değeri (`GameHeader`'da `clamp(...)` — bkz. `LOGO_HEIGHT`, 24 Temmuz 2026'da logo skor kutuları/Giriş'le aynı 375/465 uç noktalı sisteme bağlandı, önceden sabit 28px'ti ve geniş ekranlarda orantısı bozulup küçük kalıyordu) olabilir; string verildiğinde SVG width/height attribute'ları yerine CSS `height` + `aspect-ratio` kullanılır (clamp()/calc() bir SVG length attribute'u olarak geçersiz). Caveat artık bir web fontu değil; yalnızca derleme-zamanı statik görsel üreten scriptlerde (`generate-logo.mjs`, `generate-icons.mjs`, `generate-og-image.mjs` — bunlar `public/logo.svg`/`.png`/`.jpg`, favicon/app icon'lar ve `og-image.png` üretir, hiçbiri sitede canlı kullanılmaz) kullanılıyor, tarayıcıya hiç gönderilmiyor.
- **Modal erişilebilirliği (`useModalA11y`, `src/hooks/useModalA11y.ts`)** — PageSpeed'in "Erişilebilirlik" denetiminde manuel kontrol gerektiren maddelerden (odak yönetimi, Escape ile kapatma, `role="dialog"`) yola çıkarak eklendi. Oyun mobil/dokunmatik için tasarlandığından **Board/Rack/Tile'a klavye desteği bilinçli olarak kapsam dışı bırakıldı** (13×13 tahtada klavyeyle taş seçip yerleştirmek gerçek bir ihtiyaç değil, maliyeti yüksek/faydası düşük); sadece modal/dialog katmanı ele alındı. Hook açılışta içeri odaklanma, Tab ile döngüsel odak hapsi, kapanışta tetikleyici öğeye geri dönüş ve Escape ile kapatma sağlıyor; paylaşılan `Modal.tsx`'e (dolayısıyla onu kullanan ~15 modal bileşenine) ve App.tsx'teki üç dahili onay popup'ına (`invasionConfirm`, `showExitConfirm`, `showPassConfirm` — hepsinde zaten "Vazgeç" olduğundan Escape ona bağlandı), Setup.tsx'teki misafir/giriş uyarısına ve AdminDashboard'un ana paneli + geri bildirim silme onayına uygulandı. **Vazgeç/iptal seçeneği olmayan dialoglar** (Setup'ın "Giriş Yap / Oyna" uyarısı gibi) için ayrıca gerçek bir "sadece kapat" handler'ı ve görünür bir X butonu eklendi — Escape'in "Oyna"ya (ya da başka bir yan etkili butona) basılmış gibi davranması yanlış olurdu. Vazgeç'i zaten olan dialoglara (AdminDashboard'un geri bildirim silme onayı gibi) ayrıca X eklenmedi, gereksiz. Aynı işin bir parçası olarak `<main>` (Setup + oyun ekranı, `App.tsx`) ve `<header>` (`GameHeader`, zaten vardı) landmark'ları eklendi; skip-to-content linki ise bilinçli olarak eklenmedi — Board/Rack/Tile klavyeyle oynanamadığından (yukarıdaki kapsam dışı kararı), header'daki birkaç durağı (logo, skor kutuları, hesap menüsü) atlamanın gerçek bir kazancı yok, `<main>`'e ulaşsa bile klavye kullanıcısını asıl oyun deneyimi değil sadece Oyna/Pas Geç gibi birkaç buton karşılıyor.
  **İç içe açılan modaller** (AuthModal→Kullanım Koşulları/Gizlilik, FeedbackModal→AuthModal, ScoreCard/PlayerScoreCard→GameHistoryModal/Leaderboard, GameOver→FeedbackModal/MoveHistoryModal gibi — bunlarda alttaki dialog kapanmadan üstte bir yenisi açılıyor) için hook modül seviyesinde bir dialog yığını tutar; yalnızca en son açılan (en üstteki) dialog Escape/Tab'ı yakalar — aksi halde her dialog bağımsız dinlediğinden Escape üsttekiyle birlikte alttakini de kapatırdı. İlk implementasyonda bu yığına giriş/çıkış `onClose`/`closeOnEscape` de efektin bağımlılık dizisindeydi; ama bunlar çoğu çağrı yerinde satır-içi fonksiyon olduğundan (her render'da yeni referans), ebeveyn bileşen her render olduğunda (ör. AdminDashboard'un sık veri/arama state güncellemeleri) dialog yığından çıkıp tekrar giriyor, üstte başka bir dialog açıkken bu dialogu yanlışlıkla yığının tepesine geri taşıyabiliyordu. Düzeltme: `onClose`/`closeOnEscape` artık bir ref'te tutulup yalnızca tuşa basıldığı anda okunuyor; yığına giriş/çıkış sadece `active` (dialog açık mı) değiştiğinde oluyor — yeni bir dialog eklerken bunu bozmamak için `active` parametresini gerçek açık/kapalı state'ine bağlı tutmaya, `onClose` içine yan etkisiz bir "kapat" fonksiyonu vermeye dikkat et.
- **`TermsModal`/`PrivacyModal`** — 27 Temmuz 2026'da Cinsiyet/Doğum Tarihi/Profil Fotoğrafı ve Arkadaşlık Sistemi ile senkronlandı: "Toplanan Veriler" listesine bu alanlar eklendi; "Veri Paylaşımı" bölümüne ad/takma isim/profil fotoğrafının Sanal Lig ve arkadaşlık aramasıyla diğer KAYITLI kullanıcılara, paylaşılan oyunların ise (`/game/:id`) girişsiz ziyaretçiler dahil herkese görünür olduğu eklendi; "Çerezler ve Yerel Depolama" bölümündeki "hiçbir izleme teknolojisi kullanılmıyor" iddiası, `guest_visits` anonim ziyaretçi sayacını (bkz. "Büyüme > Kullanıcı") da dürüstçe kapsayacak şekilde düzeltildi (reklam/pazarlama amaçlı değil, kimlikle eşleşmeyen anonim bir sayaç olduğu netleştirildi — önceden bu sayaç var olduğu halde politika "hiçbir izleme yok" diyordu, bu bir tutarsızlıktı). Kullanım Koşulları'nın kapsam listesine "arkadaşlık" eklendi, yasaklı kullanım listesine arkadaşlık/davet özelliklerinin spam amaçlı kullanımı eklendi. Bu iki dosya, yeni bir kullanıcıdan veri toplayan ya da kullanıcı verisinin görünürlüğünü değiştiren her özellik eklendiğinde (`CLAUDE.md`/`README.md` güncellemesiyle aynı refleksle) tekrar gözden geçirilmeli.
  **19 Ağustos 2026 — denetim: beş madde (kullanıcı sordu: "Terms and privacy güncel mi?").** Tarihler web ↔ port zaten tutarlıydı (`legal_text_test.dart` bunu zorluyor), ama İÇERİK beş yerde koddan kopmuştu; beşi de aynı PR'da düzeltildi ve iki tarihi de **19 Ağustos 2026**'ya çekildi (port `legal_modals.dart` aynı PR'da — bayat kalsa mobil CI düşerdi).
  1. **"Takma isim (isteğe bağlı)" ARTIK YANLIŞTI** — `display_name` 29 Temmuz 2026'dan beri `NOT NULL` + benzersiz (`nickname_required_unique`) ve iki form da "Takma isim zorunludur." diyor. → "Takma isim (zorunlu — oyunlarda ve k-lig'de herkese görünür)".
  2. **Yalnızca Supabase adı geçiyordu; Brevo ve Vercel HİÇ geçmiyordu.** Oysa 11 Edge Function ad + e-posta adresini Brevo'ya gönderiyor ve site Vercel'de barınıyor — aktarılan alıcı kategorilerini saymamak eksik aydınlatma (KVKK m.10). 4. bölüm artık üç sağlayıcıyı da, hangisine NE gittiğini söyleyerek sayıyor.
  3. **"Hesabınızı silmeniz durumunda…" var olmayan bir yolu vaat ediyordu** — ne web'de ne portta hesap silme UI'ı var (`grep` ile doğrulandı; KVKK "unutulma hakkı" özelliği bilinçli olarak ertelenmişti, bkz. "Hesabı Devre Dışı Bırakma" notu). 5. bölüm artık silmenin 8. bölümdeki "Görüş Bildir" kanalından TALEP edildiğini ve 30 gün içinde yerine getirildiğini söylüyor. **Self-servis silme hâlâ YAPILMADI** — "Sonraya Bırakılan Ürün Fikirleri"ne madde olarak eklendi.
  4. **"Görüş Bildir" formu "Toplanan Veriler"de hiç yoktu** — `feedback` tablosu serbest metin mesajı + e-posta adresi saklıyor (misafirden de), admin okuyor ve yanıt e-postası gönderiyor. Yeni madde eklendi.
  5. **Terms'in kapsam cümlesi bayattı** ("oyun tahtası, lider tablosu, arkadaşlık ve kullanıcı hesabı") — Canlı oyun ve oyun içi mesajlaşma sayılmıyordu (oysa 3. ve 5. maddeler mesajlaşmayı zaten düzenliyordu). Kapsam ikisini de içerecek şekilde genişletildi.
  **Doğru çıkıp DOKUNULMAYANLAR (ölçüldü/okundu, bir sonraki denetim tekrar aramasın):** "üçüncü taraf font çağrısı yapılmaz" — `index.html`de yalnız Supabase `preconnect` var, fontlar `public/fonts`tan; anonim ziyaretçi sayacı, `?ref=` kaynak etiketi, `games.platform`, sohbet arşivinin katılımcı+admin kilidi, sessize alma/şikayet görünürlüğü, pazarlama onayı ve geri çekme yolu — hepsi koda uyuyor. **Sınırda bırakılan:** `games.moves` (hamle dökümü, 12 Ağustos) ayrı bir madde olarak yazılmadı — "oyun istatistikleri" başlığının altında sayılıyor; ileride istenirse açıkça yazılabilir.
  **Ders:** bu iki metnin bayatlaması sessiz — derleyici, test ve kullanıcı hiçbiri fark ettirmiyor; `legal_text_test.dart` yalnızca web ↔ port TARİH eşitliğini koruyor, metnin ÜRÜNE uyduğunu hiçbir şey kontrol etmiyor. Yeni kullanıcı verisi ya da yeni bir üçüncü taraf sağlayıcı eklendiğinde bu iki dosya migration senkronu kadar zorunlu bir adım sayılmalı.

  **29 Temmuz 2026 — pazarlama iletişimi onayı ile senkronizasyon:** "Toplanan Veriler"e "Pazarlama iletişimi onayı ve onay tarihi (isteğe bağlı)" eklendi; "Verilerin Kullanım Amacı" listesine "Yalnızca ayrıca onay verdiyseniz: pazarlama/tanıtım amaçlı iletişim" eklendi. Daha önemlisi, "Veri Paylaşımı ve Aktarım" bölümündeki "Kişisel verileriniz; reklam, pazarlama... amacıyla kullanılmaz" cümlesi artık BU ÖZELLİKLE ÇELİŞİYORDU (yalan olurdu) — bu yüzden cümle, açık rıza istisnasını (kayıt formundaki onay kutusu, isteğe bağlı, işaretlemeden de hizmet eksiksiz kullanılabilir) belirtecek şekilde güncellendi. Geri çekme yolu için ilk sürümde 8. bölümdeki "Görüş Bildir" başvuru akışına yönlendiriliyordu (o an `AccountSettingsModal`'da henüz bir toggle yoktu) — aynı gün ikinci değişiklikte (yukarıdaki `AuthModal` notuna bkz.) gerçek bir self-servis toggle eklenince cümle "Hesap Ayarları'ndaki aynı onay kutusundan istediğiniz zaman verebilir ya da geri çekebilirsiniz" şeklinde güncellendi. "Çerezler ve Yerel Depolama"daki "reklam/pazarlama amaçlı çerez/izleme teknolojisi kullanılmıyor" iddiasına DOKUNULMADI — bu iddia hâlâ doğru, çünkü marketing_consent bir çerez/izleme teknolojisi değil, sıradan bir profil alanı.

### Port dalında mahsur kalan web düzeltmeleri (10 Ağustos 2026)

**Sorun bir kod hatası değil, bir TESLİM hatası.** Flutter portu üzerinde çalışırken `src/` altında yapılan web düzeltmeleri port dalında (`claude/word-game-flutter-port-dr9dos`) kalıyor; `main`'e ancak ayrıca, main tabanlı küçük bir PR'a kopyalanırsa ulaşıyor. Bu ay #226-#229 böyle gitti — ama iki düzeltme kopyalanmayı atladı ve haftalarca üretimde eksik kaldı:

| Düzeltme | Yapıldığı tarih | Canlıya geliş |
|---|---|---|
| `GameHistoryModal`/`SharedGamePage`: "SL" → **k-lig** | 6 Ağustos | 10 Ağustos akşamı (kullanıcı fark etti) |
| `AdminChatTranscriptModal`: mesajlar en yeni üstte | 9 Ağustos | 10 Ağustos akşamı (aynı denetimde bulundu) |

İkincisi daha sinsi: 9 Ağustos'ta "mesajlar HER YERDE en yeniden eskiye" kuralı ÜÇ ekrana birden uygulanmıştı (`ChatModal`, `GameChatHistoryModal`, `AdminChatTranscriptModal`), ama `main`'e ikisi gitti, admin dökümü gitmedi — yani tam da kapatılmak istenen tutarsızlık bu sefer web↔port arasında yeniden doğdu ve kimse fark etmedi (admin dökümü nadir açılıyor).

**Denetimin doğru yolu — tek komut:** `git diff --name-status origin/main..<port-dalı> -- . ':!mobile'`. Bu, port dalındaki mobil-DIŞI her farkı listeler. Çıkanları üçe ayır: (a) kullanıcıya görünen web düzeltmesi → **hemen main'e ayrı bir PR ile**, (b) port altyapısı (`scripts/generate-golden-vectors.ts`, `generate-meanings-db.mjs`, `random.ts`'in `setRandomSource` kancası, `mobile-build.yml`, `package.json` script girdileri) → port merge'iyle gelsin, (c) doküman. **(b)'yi erken taşıma:** `generate-klig-paths.mjs`/`generate-logo-paths.mjs`'in port sürümü Dart dosyalarına da yazıyor, `mobile/` olmayan bir `main`'de çalıştırılırsa hata verir.

**Kalıcı önlem:** `mobile/CLAUDE.md`'nin "Parça Bitirme Kontrol Listesi"nin 1. maddesi `git status`ta `mobile/` dışı dosya arıyordu ama yalnızca **doküman senkronu** istiyordu — "web değişikliğini `main`'e TESLİM et" adımı yoktu. Delik orada kapatıldı.
